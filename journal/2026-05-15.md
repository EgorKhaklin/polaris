# Journal — 2026-05-15

Episodic memory for this date. Each session opens with "## SESSION
HH:MM" and closes with "## END HH:MM". Decisions and learnings are
flat-list under their session.
- **decision** 19:30 — v9.23 SHIPPED — BIG MISSION composite ship (HIGH composite; forty-eighth ship; Pattern #20 Constitutional Discipline 18th instance). VANTA's verbatim authorization (2026-05-15, in-chat): *"BIG MISSION. (Architect + Antiarchitect Agents discusses each one, improve them / add on to them, maybe do more things, maybe dont do some. If you find anything, you can also add to the mission list, and remove from the mission list if you dont think its the right fit. You can use the HYdra and the swarm, and everything in polaris, Vanta Sanctum authorized"* — 12-item mission list across 3 priority tiers (Critical 3 / High 4 / Medium 5) with explicit authorization for Architect + Anti-Architect to debate scope. The protocol fired on a real strategic question for the third time (first v9.12 polaris-odyssey-debate; second v9.16 open-arcs-debate). Single composite Sanctum `sanctum/2026-05-15-big-mission.md` records the 12-item debate + joint resolutions + 6-of-8 anti-pattern hits (AP1, AP3, AP4, AP6, AP7, AP8) + VANTA's verbatim authorization. **The Anti-Architect's dissent materially shaped 9 of 12 items:** refused multi-region per v9.16 RESERVED-NOT-PLANNED → DR-SINGLE-REGION.md; refused broad TLA+/Lean scope (AP7 + AP1; Hypothesis tests already cover C1/C2/C3) → ONE C3 demonstrator spec only; refused external red-team simulation (AP8) → scope document only, operator commissions actual exercise; refused QuantumObserverBinding half-implementation (AP7 + AP8) → mirror v9.16 RESERVED-NOT-PLANNED pattern; refused rebuild of WebAuthn substrate (AP3; v8.97 already 100% complete at 459 lines) → operator-facing deadline-set helper + rollout runbook only; refused new RASP "framework" framing → rule catalog with implemented-vs-gap status; refused new archival infrastructure → cron-install glue wiring existing v8.84/v8.87/v9.07 scripts; refused proposing mitigations for every cognitive threat (AP3) → threats NAMED, mitigations cataloged as proposals; refused "shifting left" / threat-modeling consulting framing on red-team → engagement-shape anti-pattern checklist baked into RED-TEAM-SCOPE.md. **Items shipped as-requested:** 3 (Critical #2 cognitive threat model, Medium #3 onboarding docs, Medium #5 CONTRIBUTING+SECURITY). **Items shipped scoped-down:** 6. **Items shipped refused-but-corrected:** 2 (multi-region → single-region; Quantum → deferred-rationale). **Items shipped extended:** 1 (load-test gained honest-accounting clause). **Items added by agent:** 0 (VANTA's "you can also add" authorization not exercised; 12-item scope already covered v9.23 gaps). **Items removed by agent:** 0. **13 new artifacts** = sanctum + 2 top-level (CONTRIBUTING.md + SECURITY.md) + 3 DEVNOTES (threat-model-cognitive + rasp-rules + quantum-observer-deferred) + 5 docs (QUICKSTART + ARCHITECTURE-OVERVIEW + RED-TEAM-SCOPE + operator/WEBAUTHN-ROLLOUT + operator/DR-SINGLE-REGION) + 2 meta/tla (c3-one-active-token.tla demonstrator + README) + meta/cognitive-threat-review-due.txt + 3 scripts (polaris-set-webauthn-deadline + polaris-loadtest-tokens + polaris-cron-install). **Plus:** polaris-restore.sh hardened with `--verify-schema-version` flag + EXIT_SCHEMA_MISMATCH=10 + step 6.5 schema cross-check. **TestWave23V923** adds 32 invariants pinning every artifact + the Sanctum's constitutional record + the v9.16 RESERVED-NOT-PLANNED honoring + the Anti-Architect's refusal vocabulary. **Vocation alignment:** 11/12 ANTI-COERCION-DIRECT/INDIRECT/INFRASTRUCTURE; 1/12 NEUTRAL (Quantum deferred); 0/12 negative. The mission as a whole strengthens the anti-coercion vocation. **The pattern of this ship:** ambitious mission scope passed through Architect + Anti-Architect debate emerged as a coherent, vocation-aligned, multi-Sanctum-respecting ship. No prior Sanctum was violated (v9.16 multi-region honored; v9.12 foresight vocation-alignment honored; v8.97 WebAuthn Position B honored; v9.11 vocation framing honored). `POLARIS_VERSION` bumped 9.22→9.23. **The BIG MISSION shipped.**
- **decision** 18:00 — v9.22 SHIPPED — landing-page repair (LOW; forty-seventh ship). VANTA: "in the demo, why were only c1,2,3,10 included also,, OPERATIONS.md... those have button/links on them that lead to 404." Two real bugs caught while reading the public landing page. **Architect + Anti-Architect debate in-flight:** Architect's first instinct on constraint coverage was to add C4-C9 as additional claim cards; Anti-Architect refused (AP3 — turning the 4-card highlight into a 10-card feature list defeats its job as anonymous-visitor introduction). Joint convergence: keep 4 highlight cards; add one paragraph after naming C4-C9 + linking to MISSION.md for canonical text + linking to MISSION.md §Vocation for v9.11 anti-coercion framing. On broken links: Architect briefly considered adding a Flask markdown-rendering route; Anti-Architect refused (AP3+AP7 — for 8 links, building a renderer is overkill; v9.21 demo already established GitHub-URL pattern for the one demo link). Joint convergence: extend that pattern to all 8 landing links. **Item 1 — C4-C9 honest accounting:** new paragraph after the 4 claim cards (C1 audit-of-record + C2 zero-knowledge + C3 one-active-per-individual + C10 identity-is-not-money) naming the other six in one sentence each (C4 atomic failed-login, C5 CSP no inline-scripts, C6 server-side disclosure, C7 no hardcoded crypto, C8 bounded result sets, C9 real-threading concurrency tests). Links to MISSION.md §"The hard constraints" + §"Vocation" for canonical text. **Item 2 — 8 broken /docs/*.md links replaced with GitHub URLs:** OPERATIONS.md + SECRETS.md + STORY.md + GLOSSARY.md + DATA-MODEL.md + API.md + SECURITY.md + PRIVACY.md all rewrites; same pattern as v9.21 demo fix. All 8 target files verified present in `docs/` before link rewrite. **Live verified fresh Docker rebuild:** 8 GitHub URLs render (was 0); 0 bare /docs/ links remain (was 8); all 6 <strong>C4-C9</strong> render; both MISSION.md anchor links render. **1 artifact:** landing.html. **Structural invariants:** new `TestWave22V922` (6 tests) — names_all_constraints_c1_through_c10, links_to_mission_for_full_constraint_text, has_no_broken_docs_links, uses_github_urls_for_docs, referenced_docs_actually_exist_in_repo, version pin + CHANGELOG. **Why this matters:** the landing page is Polaris's first impression for anonymous visitors. Eight 404'd links + an apparent constraint-coverage gap together read as "unfinished system." Neither was true — the constraints were all in the schema, the docs all existed in the repo. The bug was the visitor's lived experience didn't show that. Now it does. `POLARIS_VERSION` 9.21→9.22. The landing page tells the truth.
- **decision** 17:30 — v9.21 SHIPPED — demo rework + full interface verification + launcher subcommand contract verified (LOW-MEDIUM; forty-sixth ship). VANTA: "demo interface needs fully updated and reworked because its outdated. Then do a full suite test on the interface so that the database works and everything from Micro to macro works correctly... Make sure all the launchers work correctly too." **Architect + Anti-Architect debate ran in-flight stress-testing each decision.** Architect surfaced 9 outdated demo elements; Anti-Architect refused 2 (WebAuthn-MFA section out-of-scope per AP3; full visual redesign aesthetic-not-functional per AP8) and endorsed 7. Joint convergence: keep 4-step structure; rewrite content with real procedures; add anti-coercion vocation framing; reflect v9.20 audit surfaces. **The real bug uncovered:** pre-v9.21 demo showed `uc1_issue_token(...)` and `uc2_verify_token(...)` — neither procedure has EVER existed in Polaris (they were hallucinated SQL). Real procedures: `uc1_issue_and_activate(12 params)`, `uc4_activate_reserve(5 params)`, `uc8_revoke_token(5 params)`; verification flow is a direct INSERT into VerificationEvent. Visitors copy-pasting the demo's SQL would have hit immediate "function does not exist" errors. **The demo wasn't just outdated — it was lying about what Polaris does.** **Other fixes in demo.html:** test count "~400 tests + 194 structural" → "924 Python + 689 structural + 171 SQL + 19 Hypothesis"; broken `/docs/reference/DATA-MODEL.md` link → real GitHub URL; no vocation framing → opens with anti-coercion vocation + closes with vocation-aligned summary; duress code (R11-5) added to Step 2; v9.20 surfaces (requesting_purpose_text + AuditAccessLog) added to Step 3. **Full interface suite verified live via Docker:** 7 public endpoints all 200, 9 protected redirect 302 anonymously + return 200 authenticated, all 4 investigate routes 200, all 4 atlas APIs return 200 with bbox param, all security headers present (CSP+COOP+CORP+Permissions+X-Frame+X-Content-Type+Referrer), demo content verified ("anti-coercion identity substrate" in hero, real procedure names in code blocks, v9.20 fields in panels, current test count in closing). **Launcher subcommands verified end-to-end:** `doctor` (diagnostic report all OK), `up --detach` (stack up), `status` (UP), `stop` (clean shutdown), `status` (not running). v9.18 fix (open browser at `/` instead of `/login`) carries forward correctly. **3 artifacts:** demo.html (full rewrite) + TestWave21V921 (12 invariants) + version bump. **Pre-v9.21 demo issues caught by tests:** test_demo_uses_real_procedure_signatures asserts uc1_issue_token + uc2_verify_token are NOT in the file; test_demo_step3_shows_v920_surfaces asserts requesting_purpose_text + AuditAccessLog appear in Step 3 section; test_demo_no_broken_internal_doc_link asserts no `/docs/*.md` URLs. `POLARIS_VERSION` 9.20→9.21. The demo no longer lies about what Polaris does.
- **decision** 16:30 — v9.20 SHIPPED — Sanctum-class: verification-purpose lineage + audit-access audit trail (MEDIUM-HIGH; forty-fifth ship; Pattern #20 Constitutional Discipline 17th instance). VANTA: "proceed with the joint recommendation." Items 3+6 of architecture-study joint recommendation; both Sanctum-class because they modify the v8.20 audit-of-record contract. **Sanctum** `sanctum/2026-05-15-verification-purpose-and-audit-access.md` covers both as one decision (Position A; both Anti-Architect-required boundaries structurally enforced). **Item 3 — Verification-purpose lineage:** migration `2026-05-15-002-verification-purpose.up.sql` adds VerificationEvent.requesting_purpose_text VARCHAR(280) with 1..280 CHECK + GIN search index. App layer: verifications_new reads purpose from form, empty→NULL, persists in INSERT. Template: verifications_form.html adds input field maxlength=280 + anti-coercion description. Anti-coercion-direct: coerced verification leaves a stated-purpose evidentiary trail. **Item 6 — Audit-access audit trail:** migration `2026-05-15-003-audit-access-log.up.sql` creates AuditAccessLog table (access_id BIGSERIAL, accessed_at, actor_user_id FK to AppUser nullable, accessed_table CHECK-bounded enum, filter_criteria_jsonb, result_row_count) with append-only trigger reusing reject_audit_modification + 2 indexes. New security.record_audit_access(get_conn, table, filter, count) helper — fail-open by design. Wired into 4 routes: /investigate/token (logs TLE + VE), /investigate/individual (VE), /verifications (VE both cursor + page modes), /duress (DuressEvent). Anti-coercion-direct: makes silent surveillance by insiders visible — the audit's audit. **Anti-Architect's required boundaries structurally enforced:** (1) AuditAccessLog reads NOT logged — regress stops there by construction (pinned by test_app_does_not_log_audit_access_log_reads); (2) no LLM purpose classification — operator-supplied free-text only, max-length CHECK only. **Live verified end-to-end Docker fresh stack:** both migrations applied via docker-init.sh (v9.18 fix carries forward), columns present, 4 protected reads → 6 AuditAccessLog rows (TLE×1, VE×4, DuressEvent×1), purpose field renders on verifications_form. **8 artifacts.** TestWave20V920 with 14 invariants. `POLARIS_VERSION` 9.19→9.20. The audit-of-record contract is extended: stated-purpose field on verifications + meta-audit table on audit-table reads. Two anti-coercion surfaces operational.
- **decision** 15:30 — v9.19 SHIPPED — investigative surface from architecture study (items 1+2+5 of joint Architect+Anti-Architect recommendation; LOW; forty-fourth ship). VANTA: "proceed with the joint recommendation." **Item 1 — Ontology layer** (`polaris_sql/15_ontology.sql`): 6 read-only semantic views (v_ontology_individual + v_ontology_token + v_ontology_agency + v_ontology_verification + v_ontology_token_timeline + v_ontology_individual_tokens). Token view surfaces `has_duress_code` (anti-coercion property); verification view surfaces `is_zero_knowledge` (C2 invariant). **Vocation-aligned by construction:** every view is single-entity-focused; NO view aggregates across individuals (the surveillance pattern is constitutionally refused). Loaded by 00_load_all.sql; smoke-tested at load. **Item 2 — Object Card UX:** new routes `/investigate/token/<id>` + `/investigate/individual/<id>` + 2 templates. Single-token: chronological timeline (lifecycle + verification unioned) + succession chain (predecessor/successor links) + holding individual link. Single-individual: token holdings (with succession chain) + recent verifications (LIMIT 100; C8 preserved). Both @login_required; read-only investigative; distinct from operational `/tokens/<id>` + `/individuals/<id>/edit` views. **Item 5 — Authorization-as-code review** (`scripts/ai-authz-audit.sh` + `scripts/ai_authz_audit.py`): pure static analysis walking 4 surfaces (app.py decorators + 09_grants.sql + AppUser.role CHECK + tables). 4 sections: By route, By role, GRANTs, Drift/gaps. Surfaces: routes with role-gate-no-login-gate, roles referenced not in enum, tables without GRANT, public routes summary. Flags --json + --role NAME. **Live verified end-to-end on fresh Docker stack:** ontology views all loaded (6 views), Object Card routes 200 authenticated + 302 anonymous, content renders ("Investigate · Token #1"), authz-audit emits clean drift report (0 role-without-login, 0 unknown-roles, 0 ungranted-tables, 13 public routes). **5 artifacts** + version + tests. **Refused patterns documented** in CHANGELOG entry (cross-entity link analysis, notebook authoring, predictive enrichment, multi-tenant identity, object-as-API). **Structural invariants:** new `TestWave19V919` with 14 tests including `test_ontology_refuses_cross_entity_aggregation` (regex-checks view names + asserts single-entity documentation present) + `test_investigate_reads_ontology_views` (consumer-side check that the routes use the ontology). `POLARIS_VERSION` bumped 9.18→9.19. Next: v9.20 Sanctum-class ship for items 3 + 6 (verification-purpose lineage + audit-access audit; both schema-touching, both anti-coercion-direct).
- **decision** 14:30 — v9.18 SHIPPED — two launcher bug fixes diagnosed live (LOW-MEDIUM; forty-third ship). VANTA: "get this error when I log in: 500 Something went wrong... also I noticed that when I launch the program, it doesn't launch the overview part." **Bug 1 — 500 on login:** root cause `column "webauthn_required_after" does not exist` — v8.97 WebAuthn migration was never applied during Docker init. Pre-v9.18 `docker-init.sh` only ran 00_load_all.sql; the polaris_sql/migrations/ directory was mounted into the container but ignored. Fresh containers therefore had the baseline schema WITHOUT any post-v8.95 migrations. The fix: after 00_load_all.sql, docker-init.sh now loops through migrations/*.up.sql in lexicographic order, applying each in a transaction that also INSERTs into schema_version (SHA-256 recorded, event_type='applied', actor_user_id=NULL since system-applied at boot). Mirrors scripts/polaris-migrate.sh discipline exactly. **Bug 2 — launcher opens /login instead of /:** polaris_mac_launch.sh had 4 open_browser calls all pointing at /login. The public landing page at / introduces Polaris and is the natural first-impression surface; sending operators straight to a login form bypassed that intro. Fix: all 4 open_browser calls changed from /login to /. The /login URL is still used by wait_for_url as the "stack is up" health probe (it's fast + public + predictable) — only the BROWSER OPEN target changed. Side effect: authenticated users hitting / are auto-redirected to /dashboard by the existing home route, so change is win/win — anonymous gets landing page, authenticated gets dashboard directly. **Live verification end-to-end against fresh Docker:** `docker compose down -v` (destroyed volume) → `docker compose up -d --build` (fresh init) → init logs showed `Applying schema migrations... ✓ applied: 2026-05-14-001... ✓ applied: 2026-05-14-002-operator-webauthn... Applied 4 migration(s).` → AppUser.webauthn_required_after column exists → POST /login with admin/Admin@123! → 302 (was 500 pre-v9.18) → GET /dashboard → 200 → GET / anonymous → 200 (landing page now opens by default). **4 artifacts:** docker-init.sh + polaris_mac_launch.sh + TestWave18V918 + version bump. **Structural invariants (6 tests):** test_docker_init_applies_migrations + test_docker_init_uses_per_file_transaction + test_launcher_opens_landing_page (counts open_browser /login = 0 + at least one / present) + test_launcher_still_waits_on_login_for_health (preserves /login as health probe) + version pin + CHANGELOG entry. **Pattern (extending v9.17's):** v9.17 caught the missing-Dockerfile-modules half of the launcher path; v9.18 caught the missing-schema-migrations half. The shared diagnostic: interface verification that bypasses the Docker stack misses both bugs. v9.13 + v9.16 ships verified via host-side gunicorn and never exercised Docker init. This pair (v9.17 + v9.18) is the cost of that gap. Filed as next candidate for ai-test.sh extension: a `--docker` mode that runs end-to-end through docker compose up. `POLARIS_VERSION` bumped 9.17→9.18. The launcher works correctly.
- **decision** 13:45 — v9.17 SHIPPED — launcher bug fix; Dockerfile + Dockerfile.prod missing webauthn_auth.py + __version__.py (LOW; forty-second ship). VANTA pasted launcher error log: `ModuleNotFoundError: No module named 'webauthn_auth'` from `Polaris.command` invocation. **Real production bug, latent since v8.97 (2026-05-14):** the WebAuthn-MFA module was added to repo + imported at startup by app.py, but was NEVER added to either Dockerfile's COPY line. Same applied to v9.06 canonical-version module __version__.py. Docker image built fine (no compile-time check) but Gunicorn worker crashed during boot. Direct-gunicorn-against-host invocations (used during v9.13 interface testing + v9.16 verification) imported from host filesystem so passed — the Docker path was never exercised end-to-end. **Fix:** both `polaris_web/Dockerfile` + `polaris_web/Dockerfile.prod` COPY lines extended with `webauthn_auth.py __version__.py` (with `--chown=polaris:polaris` preserved on Dockerfile.prod). **Live verified:** rebuilt image, restarted stack on launcher's port 2222, /api/health → 200, / → 200, /login → 200, no ModuleNotFoundError in container logs. **Regression guard (real payoff):** new TestWave17V917 class adds three invariants — `test_dockerfile_copies_webauthn_auth` (literal check), `test_dockerfile_copies_version_module` (literal check), and the **generic** `test_dockerfile_covers_all_runtime_app_modules` which parses app.py for top-level `import <name>` lines, verifies each is a real file in polaris_web/, then asserts every such module appears in both Dockerfiles. This catches ANY future module-added-without-Dockerfile-update gap, not just this specific bug. **4 artifacts:** Dockerfile + Dockerfile.prod + TestWave17V917 + version bump. **Anti-Architect endorsement by silence:** canonical bug-fix-with-regression-test ship. AP1 does NOT fire (fix touches Dockerfile = infrastructure for Layer-1 product modules); AP3 does NOT fire (generic detector serves operator long after this specific bug forgotten). **Pattern observation:** v9.13 + v9.16 interface verification bypassed Docker; both missed this bug. Lesson noted for future ai-test.sh extension. `POLARIS_VERSION` bumped 9.16→9.17.
- **decision** 09:30 — v9.16 SHIPPED — open-arcs debate resolved as Position C′ (joint Architect + Anti-Architect recommendation; MEDIUM; forty-first ship; Pattern #20 Constitutional Discipline 16th instance — first instance where a debate resolved by NOT opening additional Sanctums). VANTA: "I wanted to point out all the opne arcs, empire, denarius, etc... should we remove some of those, do soemthing with them, or what. suggest best course of action with the architect + anti-architect." Then: "proceed with joint recommendation." The v9.11 protocol fired on a real strategic question for the second time (first was v9.12 polaris-odyssey-debate). **Architect surfaced four positions** (A close-out via 3 Sanctums; B reservation pattern broadly; C two-track; D leave-as-is). **Anti-Architect named AP2** (Sanctum-overuse on A), **AP7** (premature abstraction on B; twelfth-legion reservation was earned by ONE case, not freely generalizable), endorsed C's close-side, **proposed RESERVED-NOT-PLANNED framing** for Arc G to distinguish from "deferred" (which implies *planned but not yet*). Both voices converged on **Position C′** — Position C with Anti-Architect's Arc G refinement. **Four arc status updates:** **Arc E (Mycelium) → CLOSED 2026-05-15** — all E-phases shipped through v8.62 → v9.11; swarm operational. **Arc F (Denarius) → CLOSED 2026-05-15** — F1-F5 all shipped (F1 Treasury+Quaestor v8.69, F2 chaos test v8.70, F3 cohort growth v8.70, F4 Cursus Honorum v8.71, F5 steady-state exemption v8.73). **Arc B (Production) → truth-updated** — Status: "Phase 1 SHIPPED v8.77; Phase 3 Wave 1 SHIPPED v9.01; Phase 2 + remaining Phase 3 GATED on production-scale triggers"; triggers explicitly named (≥10× verification volume, scaling incident, partner deployment, federation requirement). **Arc G (Empire) → truth-updated with RESERVED-NOT-PLANNED framing** — Phase 1 SHIPPED v8.71; Phase 2/3 RESERVED-NOT-PLANNED (distinct vocabulary from v9.11 twelfth-legion RESERVED pattern to honor AP7); manifestation protocol documented inline. **Single authorizing Sanctum** `sanctum/2026-05-15-open-arcs-debate.md` covers all four arcs (Anti-Architect AP2 cost-naming honored by close-by-doc-edit rather than per-arc closing Sanctums). **6 artifacts:** Sanctum + 4 arc-docs + sanctum-index. **Structural invariants:** new `TestWave16V916` with 10 ship-specific invariants pinning every new Status line + single-Sanctum-for-four-arcs constraint + RESERVED-NOT-PLANNED framing + F1-F5 enumeration + Anti-Architect shaping record. `POLARIS_VERSION` bumped 9.15→9.16. **The arc-status drift is closed.** v8.20 audit-of-record discipline restored: no more "active" labels on functionally-complete arcs; deferred work has named real-world triggers; speculative work is RESERVED-NOT-PLANNED with explicit manifestation protocol.
- **decision** 08:30 — v9.15 SHIPPED — full Mycelium surface in brain-map (LOW; fortieth ship). VANTA: "what if we add the mycelium to to the brain-map main." Pre-v9.15 the brain-map had HYDRA's 9 watchers but only stubs for the swarm tier (priest soldier alone + reserved-twelfth-legion slot). v9.15 adds 5 new collectors to `scripts/ai_brain_map.py`: `parse_legions` (11 manifest: 9 Republican + 2 Imperial), `parse_commander_ants` (every ant clustered under its legion via `serves_in` edge), `parse_worker_soldiers` (8 workers; priest already in v9.11), `parse_citizens` (6 classes: Plebs, Equites, Augures, Censores, Quaestores, Tribuni Plebis), `parse_treasury` (Civitas Treasury with Quaestor `tends` edge). **Result:** brain-map node count 304→366 (+62), link count 336→373 (+37). **Division of labor post-v9.15:** brain-map = unified cross-tier view (schema + behavior + observation + cognitive + decision + constitution + knowledge — all in one force-directed graph); swarm-map = swarm-native view (substrate-center, lens-outer-ring topology). Both remain useful for different operator questions. **2 artifacts:** ai_brain_map.py + regenerated brain-map.html. **Structural invariants:** new `TestWave15V915` with 9 ship-specific invariants pinning each collector + build() wiring + JSON-data-island content. `POLARIS_VERSION` bumped 9.14→9.15. The brain-map is now the canonical unified view of the entire system.
- **decision** 07:30 — v9.14 SHIPPED — brain-map catch-up to v9.13 + new swarm-map (Mycelium-native visualization) + two HYDRA/swarm diagnostic helpers (LOW-MEDIUM operator-instrumentation; thirty-ninth ship). VANTA: "Lets update the brain map, and make sure its upto date and maybe improve it more. I was also thinking about what if we made a map for the swarm itself, and maybe think of more things that would help out the hydra + the swarm." **Brain-map catch-up:** 8 new collector methods in `scripts/ai_brain_map.py` surface v9.11-v9.13 entities (foresight package, foresight SQL helpers, action_promotion module, Anti-Architect persona, priest soldier, twelfth-legion reservation, vocation, cadences); brain-map node count 280→304 (+24), link count 321→336 (+15). **New: Swarm-map** (`scripts/ai_swarm_map.py` + `scripts/ai-swarm-map.sh` → `meta/swarm-map/swarm-map.html`) — Mycelium-native visualization, distinct from brain-map. Layout: substrate at center, HYDRA watchers on outer ring (lens), 11 manifest legions + 1 reserved on inner ring, ants under legions, soldiers + priest in own tier (priest rendered gold + distinct node type), citizens, treasury. Flags: `--open` / `--live` (per-ant deposit cadence via DB) / `--auto`. Output: 75 nodes / 111 links / live verified. **New: `ai-swarm-health.sh`** — one-screen swarm-state snapshot with 7 sections (substrate freshness, per-legion cadence, per-soldier-class with ✓/✗ silence detection, citizens, treasury, shared correlation surfaces v9.10, anomalies). Flags: `--json` / `--quick`. Gracefully degrades when DB unreachable. **New: `ai-watcher-coverage.sh`** — pure static analysis report on HYDRA watchers: per-watcher coverage (SQL tables, file refs, node IDs, scripts invoked) + Layer-1 coverage + blind spots (tables no watcher reads) + cross-watcher overlap (node IDs shared by ≥2 watchers; surfaces v9.10 shared surfaces with `[v9.10 shared surface]` tag — confirmed runtime:health: performance+security, runtime:swarm: ant_colony+cognitive). Flags: `--watcher` (focus one), `--json`. **6 artifacts:** ai_brain_map.py + ai_swarm_map.py + ai-swarm-map.sh + swarm-map.html + ai-swarm-health.sh + ai-watcher-coverage.sh. **Structural invariants:** new `TestWave14V914` class with 19 ship-specific invariants. `POLARIS_VERSION` bumped 9.13→9.14. **Operator-instrumentation ship:** every artifact is a diagnostic surface or visualization, none introduce new abstractions or architectural primitives. The HYDRA + swarm tiers now have dedicated viewing surfaces matching what the brain-map gave the system as a whole.
- **decision** 06:30 — v9.13 SHIPPED — macro-to-micro + micro-to-macro consistency sweep + production-grade security hardening + interface verification + production-mode cleanup (MEDIUM closing-pass; thirty-eighth ship). VANTA in-chat 2026-05-15: "macro to micro and micro to macro make sure that everything in the document is upto date (including all the scripts and readmes, literally evrything). Also do further fully professional security hardening. Also test all the interface features so it works. Clean up everything so its in production mode." No new architectural primitives; every change closes drift, hardens existing surface, or fixes a real bug surfaced by live interface testing. **Security hardening (production-grade additions):** Cross-Origin-Opener-Policy: same-origin + Cross-Origin-Resource-Policy: same-origin (isolation defense against Spectre-class side-channel + cross-origin object embedding); CSP `upgrade-insecure-requests` directive added when HSTS active; Permissions-Policy extended with `interest-cohort=()` + `browsing-topics=()` (Chrome Topics-API opt-out); Server header scrubbed at TWO layers (gunicorn worker-init monkey-patches `gunicorn.http.wsgi.Response.default_headers` to drop hardcoded `Server: gunicorn`; Flask `after_request` does pop+set `Server: Polaris`); `/security.txt` + `/.well-known/security.txt` routes (RFC 9116) with contact + expiration configurable via env vars. **Live interface verification:** started gunicorn against polaris_test DB + applied all migrations; verified all public endpoints (/ /demo /login /api/health /security.txt) return 200, all protected endpoints (/atlas /individuals /tokens /verifications /sql /dashboard) return 302→/login when anonymous, login flow with admin/Admin@123! + CSRF returns 302 then all protected endpoints return 200, all security headers present on every response. **Interface bug fix (real):** `polaris_web/test_app.py` PROTECTED_PATHS list included `/` from a pre-v8.x era when root was protected; live verification surfaced 200-vs-302 mismatch; v9.13 removes `/` with a comment naming why (root is the public landing-page architecture from v8.x). **Micro-to-macro drift fixes:** MISSION.md item 7 test count `795 Python` → `846 Python`, `140 SQL self-tests` → `171 SQL self-tests` (via `bash scripts/ai-test-counts.sh --update`); CLAUDE.md state-map test counts (~400→~846 Python, 415→592 structural-invariant tests, 88→171 SQL), app.py line count (~3,500→~4,085), webauthn_auth.py line count (~350→~459), soldier classes count `8 (v9.03)` → `9 (8 workers + 1 priest soldier_swarm_witness added v9.11)`; CLAUDE.md file-map adds `14_foresight_helpers.sql` line (v9.12 Position B Layer-1 bundle). **Foresight sunset clause real bug fix:** pre-v9.13 the sunset clause counted raw brief emissions, so six same-day `--save` invocations during testing falsely triggered SUNSET TRIGGERED; v9.13 changes the count to distinct calendar months via new `_distinct_months_in_briefs()` deduping by YYYY-MM prefix; operator re-running the script multiple times in one Saturn-pass now correctly counts as one. **Production cleanup verified clean:** no `# TODO`/`# FIXME`/`# XXX`/`# HACK` debt markers in any polaris_* package, no `app.run(debug=True)` paths, no debug print() calls in code paths (only operator-facing startup messages in `__main__` block); all ai-* scripts (ai-meta/ai-coherence/ai-link-check/ai-foresight/ai-anti-architect/ai-architect/ai-brain-map) smoke-tested green; brain-map regenerated. **9 artifacts** (MISSION.md, CLAUDE.md, polaris_web/security.py, polaris_web/app.py, polaris_web/gunicorn.conf.py, polaris_web/test_app.py, polaris_foresight/foresight_agent.py, polaris_web/__version__.py, polaris_web/test_structural_invariants.py). **Structural invariants:** new `TestWave13V913` class with 14 ship-specific invariants pinning every security-hardening addition + every drift fix + sunset-dedup behavior + protected-paths correction. **Verification:** full structural-invariant suite (606 tests post-v9.13) all green; ai-meta + ai-coherence + ai-link-check HEALTHY/INTACT/524-resolved; live HTTP smoke against gunicorn on :2222 all endpoints respond correctly. `POLARIS_VERSION` bumped 9.12→9.13. **The kind of ship the Anti-Architect endorses by silence**: no new abstractions, no new vocabulary, no new subsystems; every line earns its place by closing a drift or hardening a known surface.
- **decision** 05:30 — v9.12 SHIPPED — Polaris_Odyssey debate resolved as Position B (joint Architect + Anti-Architect recommendation; thirty-seventh ship; Pattern #20 Constitutional Discipline 15th instance — FIRST instance where the Anti-Architect's dissent materially shaped the final position; the v9.11 protocol working as designed). VANTA proposed Polaris_Odyssey as a complete subsystem (Quest Generator + four agent classes including "Mythic Agents" + Simulation Engine + vector DB + message queue). Then: "have the architects and the anti architect debate on what should be done and come back with their recommendation on how we should proceed and if we should." This was the first live test of the v9.11 Anti-Architect persona contesting an OPERATOR-originated proposal. **The debate ran fully:** Architect acknowledged the underlying gap is real (Polaris currently has no long-horizon discovery function) but contested the proposal's scope. Anti-Architect named SIX anti-pattern hits on the original proposal (AP1 self-observation without ground-touch — pure L2/L3 expansion thirteen days after S2 cadence-rule ratification; AP3 proposal-as-self-elaboration — Architect-maintained; AP4 pattern-projection — "trinity completes" + "higher intelligence" framing; AP5 vocation drift — research-papers/philosophy inputs don't trace to anti-coercion; AP7 premature abstraction — Quest/Agent/Simulation as services with zero concrete instances; AP8 larping — "Mythic Agents (optional esoteric layer)"). **Joint convergence on Position B** — minimum-viable foresight script with Anti-Architect modifications enforced as STRUCTURAL: §IV vocation-alignment required at Brief construction + 50%-acceptance threshold over 6 monthly briefs as empirical-graduation rule + 6-month sunset clause + no external API fetches + one ForesightAgent type only + no Mythic Agents branch ever + no Quest Generator/Simulation Engine/Agent Manager/Synthesis Bridge classes + operator-installed only. VANTA in-chat 2026-05-15: "proceed with architects recommendation" then "proceed with joint recommendation" — the two letters concordant since Position B IS the joint recommendation. **Sanctum file:** `sanctum/2026-05-15-polaris-odyssey-debate.md` (DECIDED+CLOSED Position B; documents the debate verbatim including all three positions + Anti-Architect's six AP detections + joint convergence). **Implementation:** new `polaris_foresight/` package — `__init__.py` + `brief.py` (Brief dataclass with 5-section render + §IV vocation enforcement at construction time via `__post_init__` ValueError) + `foresight_agent.py` (single deterministic ForesightAgent reading CHANGELOG/ROADMAP/sanctum/journal/macro-rescans/external_categories.txt; sunset constants `SUNSET_BRIEFS_REQUIRED=6` + `SUNSET_ACCEPTANCE_THRESHOLD=0.50`) + `promotion.py` (FS-XXXXXXXX promotion mirror of v9.11 AP-XXXXXXXX; vocation-alignment REQUIRED — `skipped_no_vocation` counter; LOW+MEDIUM only; idempotent) + `external_categories.txt` (operator-curated; 15 anti-coercion-aligned categories) + `_acceptance_log.json` (empirical-graduation tracker; schema documented in `_doc` field) + `README.md` (sunset clause prominently documented). New `scripts/ai-foresight.sh` (operator entry; `--save` writes to `journal/foresight/YYYY-MM-DD.md`; `--promote` writes top-N FS-XXXXXXXX to ROADMAP.md; `--top-n N` adjusts; `--voice` prints README). **Layer-1 bundle (cadence rule):** new `polaris_sql/14_foresight_helpers.sql` with three SQL functions — `foresight_token_age_distribution()` (returns 4-bucket histogram), `foresight_verification_dormancy(p_days)` (counts ACTIVE tokens with no verification in last N days), `foresight_audit_volume_trend(p_weeks)` (weekly TLE+VE counts trend). All three smoke-tested at load via DO-block at end of file; verified live against seeded DB (token age 0-30d×2 + 30-90d×1 + 90-365d×4; dormancy 1/3 ACTIVE; audit volume 7 events current week). Loaded by `polaris_sql/00_load_all.sql`. **Live drill verified:** `bash scripts/ai-foresight.sh --save --promote` first run promoted 1 FS-XXXXXXXX candidate (FS-5B5F30C9 anti-coercion-aligned PQC defensive scan); idempotent re-run promoted 0 (skipped_existing=1). **12 artifacts:** sanctum + 7 polaris_foresight files + ai-foresight.sh + 14_foresight_helpers.sql + 00_load_all.sql + ROADMAP.md. **Structural invariants:** new `TestWave12V912` class with 25 ship-specific invariants pinning each Anti-Architect modification (vocation enforcement at Brief construction; FS-XXXXXXXX format; idempotency; HIGH-risk refusal; vocation-required refusal; sunset constants 6/50%; no Mythic Agents anywhere; no Quest/Simulation/Agent Manager/Synthesis Bridge classes anywhere; SQL helpers exist + loaded + smoke-tested). `POLARIS_VERSION` bumped 9.11→9.12. **The protocol works:** an operator-originated subsystem proposal entered the Architect/Anti-Architect debate; the dissent materially shaped the final position; the function is shipped, the subsystem is not, the empirical-graduation path is honest, the sunset clause prevents dormant infrastructure. The proposed "Polaris_Odyssey" name is held in reserve pending the empirical-graduation threshold being met.
- **decision** 04:30 — v9.11 SHIPPED — Architect's vision adopted + Anti-Architect created (MEDIUM-HIGH composite; thirty-sixth ship; Pattern #20 Constitutional Discipline 14th instance — Tarot Temperance, the integration). VANTA in-chat 2026-05-15: "lets proceed with the architects vision and execute it. Vanta thought, what if we made an anti-architect." Composite executes the three-chapter Architect's vision (Closing the loop, Naming the vocation, Honoring the geometry) plus VANTA's proposed structural counterweight. **The Anti-Architect (VANTA's proposal):** structural counterweight to the Architect persona; reads the same state and emits a four-section dissent brief (§I retroactive cost audit, §II per-proposal contests with cost named + refusal threshold, §III anti-pattern detection on 8 catalogued AP1..AP8 patterns, §IV explicit silence). Loyal-opposition pattern made operational: Architect proposes; Anti-Architect contests; VANTA decides. New `scripts/ai-anti-architect.sh` + `meta/anti-architect.md` (persona spec + AP catalog). **Vocation named (Chapter XI):** opened + DECIDED Position A `sanctum/2026-05-15-vocation-anti-coercion.md` — anti-coercion as the deepest constraint, above C1-C10. Names what the codebase already implements (seven load-bearing primitives: TokenSignature backfill, multi-signature migration, WebAuthn-MFA, federation trust graph, redaction-proof discipline, audit-of-record, duress-codes). MISSION.md gains §"Vocation" above C1-C10. meta/architect.md gains §"Vocation alignment" + §"The Architect's shadow" (8-pattern anti-pattern catalog the Anti-Architect references). Anti-Architect AP5 detection (vocation drift) becomes operational. **ActionQueue auto-promotion (Chapter X — closing the loop):** new `polaris_hydra/action_promotion.py` + host.py CLI flags `--promote-actions` + `--promote-top-n N`. Top-N actions auto-promote to ROADMAP.md as candidate items with stable AP-XXXXXXXX IDs (sha256 of title; deterministic). Idempotent (re-runs add nothing). Conservative (LOW + MEDIUM only; HIGH still requires Sanctum). Vocation-aware (each item carries alignment hint). Decline-marker convention documented (~~AP-XXXXXXXX~~ struck-through prevents re-promotion). **Live drill verified:** first run promoted 4 new (4 skipped by severity); idempotent re-run promoted 0. **Layer-ratio refinement:** ai-architect.sh `emit_outlook` Layer-ratio awk script now counts only BACKTICKED file paths (the artifacts-list discipline) instead of any narrative mention; also excludes `__version__.py` (every ship bumps it). Result: honest metric. Pre-fix L1×5 (inflated by narrative); post-fix L1×2 (real Layer-1 ships). **Reserves honored (Chapter XII — geometry):** (a) `RESERVED_TWELFTH_LEGION_SLOT` constant in `polaris_swarm/legions/__init__.py` + `meta/twelfth-legion.md` documenting the held silence + manifestation protocol; (b) ninth soldier `soldier_swarm_witness` (the priest tier) added — gives the substrate internal self-knowledge by reading recent Pheromone deposits and emitting meta-pheromone under `witness:swarm:*`; v9.03 baseline of 8 preserved, v9.11 superset adds the priest = 9; (c) `runtime:auth` shared correlation surface (RESERVED in v9.10) now structurally pinned by TestWave11V911 — removing the reservation breaks the trinity. **Sanctum lifecycle 4-state expansion:** OPEN → DECIDING → DECIDED → SHIPPED (with CLOSED as canonical synonym for SHIPPED). Optional intermediates; backwards-compatible (every existing Sanctum status remains valid). meta/sanctum-protocol.md updated. **Cron cadence vocabulary:** `meta/cadences.md` — seven planetary names (Saturn-pass / Jupiter-pass / Mars-cycle / Sun-pass / Venus-cycle / Mercury-cycle / Moon-cycle) for the seven cadences Polaris's automated operations run on. Mnemonic, not literal. **12 artifacts:** anti-architect.sh + anti-architect.md + vocation Sanctum + MISSION.md + architect.md (×2) + action_promotion.py + host.py + ROADMAP.md + ai-architect.sh + legions/__init__.py + twelfth-legion.md + swarm_witness.py + pheromone_reader.py + sanctum-protocol.md + cadences.md. (Twelve files; the count is structurally meaningful — twelve completions mirror the twelfth-legion reservation, twelve houses, twelve months.) **Structural invariants:** new `TestWave11V911` class with 25 ship-specific invariants. **Pattern #20 Constitutional Discipline 14th instance** — Tarot Temperance, the integration. Three previously-separate concerns (vocation = direction, Anti-Architect = opposition, auto-promotion = closed loop) integrated into one composite ship. `POLARIS_VERSION` bumped 9.10→9.11. **First ship to introduce a structural counterweight persona; first ship to constitutionally name the vocation; first ship to close the observe→correlate→act loop autonomously.**
- **decision** 03:30 — v9.10 SHIPPED — Architect's recommendations adopted (S1 Position B + S2 Position C); first Layer-1 ship since v8.97 (MEDIUM composite; thirty-fifth ship; Pattern #20 Constitutional Discipline 13th instance — first instance of two Sanctums closing in a single composite surface). VANTA in-chat 2026-05-15: "proceed with the architects recommendation" — the same letter authorizes both v9.09 Sanctums (S1 watcher-node-id-alignment Position B + S2 cognitive-layer-ratio Position C). **S1 (Position B — designed shared-surface node_ids ADDITIVE):** v9.09 / C instrumentation surfaced that HYDRA's CorrelationEngine has fired 0 times in 6+ runs because watchers emit disjoint `node_id` namespaces. v9.10 implements ADDITIVE shared-surface design — findings carry optional `evidence.additional_node_ids: list[str]` alongside primary `node_id`. CorrelationEngine indexes by EVERY node_id (one finding may now appear under multiple keys). Two shared surfaces wired live: `runtime:health` (security_watcher + performance_watcher both emit on app-not-reachable findings) + `runtime:swarm` (ant_colony_watcher + cognitive_watcher both emit on swarm-tier-silent findings); `runtime:auth` RESERVED (mission_watcher does not yet emit auth-related node_ids — deferred until empirically warranted per Sanctum §IV.2 inclusion rule of ≥2 watchers). **Live drill verified**: `bash scripts/ai-hydra.sh --full --save` produces "VI. CROSS-WATCHER CORRELATIONS: [INFO] 2 watchers correlate on node runtime:health; watchers: performance, security; score: 2.0" on the very first run after implementation — satisfying Sanctum §IV.3 acceptance criterion (≥1 correlation fires within 5 HYDRA --full runs). **S2 (Position C — defer; trust emergent rebalancing with vigilance):** v9.04 → v9.08 (~85% Layer-2/3/4 work) was a deliberate cognitive-layer completion arc following v8.x Layer-1-heavy work. Architect's caution: C requires active vigilance. ROADMAP.md gains §"Layer-1 candidates" at top (3 candidates enumerated; cadence rule documented: ≥1 Layer-1 per 5 ships OR explicit Sanctum recording why the cycle remained Layer-2/3-focused). scripts/ai-architect.sh `emit_outlook` gains "Layer ratio (last 5 ships): L1×N L2×N L3×N L4×N" line computed from CHANGELOG entry text (operator-grep-friendly drift detector). **Layer-1 ship (S2 §III architect's required v9.10 component):** `polaris_sql/08_tests.sql` gains §S — 10 SQL self-tests for the v9.07 Pheromone rotation framework (S.1 raw DELETE rejected, S.2 raw UPDATE rejected, S.3 GUC carve-out allows DELETE, S.4 carve-out evaporates without SET LOCAL, S.5 LifecyclePheromoneCheckpoint append-only DELETE, S.6 archive_sha256 hex CHECK, S.7 cutoff_in_past CHECK, S.8 rows_purged_nonneg CHECK, S.9 admin-role gate, S.10 actor-exists gate). Section S gates on framework presence via runtime setting `polaris.test_pheromone_framework_present` (skipped on fresh load before `polaris-migrate.sh --up` applies the v9.07 migration). **All 10 section S tests PASS on both paths.** **Both Sanctums DECIDED + CLOSED** with §V/§VI filled. **11 artifacts:** correlation.py + 4 watchers + 2 Sanctums + DEVNOTES (Shared correlation surfaces section) + ROADMAP (Layer-1 candidates section) + ai-architect.sh (Layer-ratio line) + 08_tests.sql (section S). **Structural invariants:** v9.09's `*_open` tests rewritten as timeless-properties tests (Sanctum exists + enumerates Position A/B/C); new `TestWave10V910` class with 16 ship-specific invariants. **Pattern #20 Constitutional Discipline 13th instance** (S1 + S2 close in same ship — first instance of two Sanctums closing in a single composite surface). `POLARIS_VERSION` bumped 9.09→9.10. **First Layer-1 ship since v8.97 WebAuthn-MFA (5 days, 12 ships gap closed).** Filed for next-ship-or-later: `runtime:auth` shared surface (deferred until mission_watcher emits auth-related node_ids per Sanctum §IV.2 inclusion rule).
- **decision** 02:30 — v9.09 SHIPPED — Multi-agent activation + 9 patches + 2 Sanctums opened (MEDIUM composite; thirty-fourth ship). VANTA: "Activate all the agents, the hydra, the architect, the swarm and scan the whole system for gaps, and then find patches... Boil the ocean." Multi-agent activation in parallel: HYDRA --full --save (9 watchers, 6 healthy + 3 drift + 0 alert) + Architect (10/10 constraints + suggestions) + ai-meta (HEALTHY) + ai-coherence (STRUCTURE INTACT) + ai-link-check (496 references resolved) + ai-treasury-report (penalty:reward 10.70:1; trajectory worsening) + ai-loop-check (3 signals; heavy-day artifacts not real loops) + ai-test-counts (DRIFT: 763→795) + ai-dashboard (7 sections + soldier silence in dev). Synthesized 11 surfaced gaps into `meta/polaris-self-roadmap-3-2026-05-15.md` (~280 lines). 9 autonomous-eligible items shipped + 2 Sanctum-class questions opened. **9 patches:** **A** MISSION test-count drift (763→795 + structural test loosened from `==` to ratio ≥ 0.7 — drift no longer trips on every ship adding invariants). **B** HYDRA brief gains Section X persistent — `BriefDelta` extended with `persistent_findings` + `persistent_actions` (intersection of prior + current title sets); `host.py:_print_full()` renders Section X when persistent items exist; the missing symmetry — items present in BOTH prior + current = stuck (operator never acted, OR unactionable, OR permanent state). **C** CorrelationEngine silence instrumentation — when 0 correlations, brief shows Strategy 1 + Strategy 2 counts (N watchers emitting node_ids; M unique nodes; K shared by ≥2; D domains; S shared by ≥3) + diagnosis "→ disjoint node_ids; correlation requires overlap"; surfaces S1 Sanctum inline. **D** Dashboard new section 8 "Self-monitoring" surfaces ai-meta + ai-coherence + ai-link-check inline (one-line ✓/!/✗ each); skipped under `--quick`. **E** ai-brain-map.sh gains `--auto` flag: regenerates iff source mtime > brain-map mtime via cross-platform stat (macOS -f%m / Linux -c%Y); silent on no-op (cron-friendly); one-shot regen on missing file. **F** ai-sanctum.sh gains `search <topic>` 3-tier ranked search: Tier 1 filename slug match (with status — DECIDED/CLOSED/OPEN/REJECTED) + Tier 2 §I "The Matter" body match (with snippet) + Tier 3 §V "Decision" body match (with snippet); de-dupes Tier 2/3 against Tier 1; CYAN color added to script. **G** new structural invariant `test_g_pre_commit_hooks_reference_existing_scripts` parses .pre-commit-config.yaml's entry: lines + asserts every `bash scripts/<name>.sh` reference resolves to an existing executable; would have caught a script-rename-without-hook-update at pre-commit time. **H** ai-hydra.sh gains `--gc` mode for journal/hydra/ rotation: lists briefs older than `--gc-keep` (default 30) + asks `--gc-yes` to confirm purge; C1 preserved by confirmation gate; suitable for cron via `--gc-yes`. New `_cli_gc()` function in host.py independent of Hydra invocation; help text updated. **N1** em-dash pre-commit hook scoped to NEW lines via `git diff --cached` — existing 290 em-dashes across own-prose docs (CLAUDE 42 + MISSION 83 + ROADMAP 132 + BACKLOG 32 + README 1) stay per v8.20 AoR; new additions blocked; operator can `pre-commit run --skip em-dash-block-new` if false-positive; `files:` pattern restricts to the 5 own-prose docs. **2 Sanctums opened:** **S1 — sanctum/2026-05-15-watcher-node-id-alignment.md OPEN** with 3 positions filed (A: accept disjointness; B: design 2-3 shared-surface node_ids ADDITIVE — runtime:swarm/auth/health — architect-recommended; C: cross-watcher correlator soldier — wrong tier). Surfaced because CorrelationEngine has fired 0 times in 6+ HYDRA --full runs; v9.09 / C instrumentation now shows why. Inclusion rule: ≥2 watchers must have been observed (in 2+ HYDRA runs) emitting findings about a concern before shared surface added. Acceptance: ≥1 correlation fires within 5 HYDRA --full runs after impl. **S2 — sanctum/2026-05-15-cognitive-layer-ratio.md OPEN** (HIGH-risk) with 3 positions filed (A: declare cognitive layer COMPLETE + freeze for Layer-1 work; B: per-ship Layer-1-minimum budget; C: defer — current ratio is correct for the v9.x cognitive-layer completion arc; v9.10+ naturally returns to Layer-1 — architect-recommended). Empirical observation: ~85% of v9.04→v9.08 effort touched cognitive-layer scaffolding (HYDRA + Mycelium + scripts/ + meta/ + docs/) and ~15% touched the actual identity-token product (polaris_web/sql/zk/cli); zero schema tables / use cases / SQL procedures / ZK circuits added since v8.97 WebAuthn. The hermetic principle: as above, so below — if the lens at the top is healthy and the substrate at the bottom is static, the symmetry is broken. Position C requires operator vigilance. **16 new structural invariants in TestWave9V909 (510→526)**; all 564 tests pass. **Constitutional preservation verified end-to-end**: C1 (no AoR rewrite — Sanctums OPEN; persistent_actions additive; em-dash hook scoped to NEW lines; --gc requires --gc-yes confirmation); C10 (no PII path); G1 (deterministic — pure additions or instrumented outputs); G15 (preserved verbatim; --gc gated by confirmation per filesystem-AoR principle); v8.20 audit-of-record (100% preserved — both new Sanctums + the roadmap-3 doc are append-only AoR). **Live drill verified**: ai-sanctum.sh search hydra returns 3 tiers (5 slug matches + 7 matter matches + 0 decision matches with snippets); ai-hydra.sh --watcher cognitive --json output contains hydra_brief_archive_status + hydra_brief_count + age_days + thresholds + status; polaris_web.__version__.POLARIS_VERSION returns '9.09'; 564 tests pass (548 prior + 16 new TestWave9V909). **Pattern #20 Constitutional Discipline 12th + 13th instances pending** (when S1 + S2 close). The v9.x cycle is now at v8.84/v8.87/v8.90/v8.91/v8.94/v8.95/v8.96/v8.97/v9.04/v9.06/v9.07/v9.08/v9.09 (S1+S2 OPEN). `POLARIS_VERSION` bumped 9.08→9.09. **The multi-agent scan→synthesize→patch cycle worked end-to-end (Pattern #18 Empirical Iteration second run today)**. The cognitive layer observed itself, surfaced gaps, shipped patches, opened the constitutional questions it couldn't answer alone. Wave-4's "ongoing observational" promise is now operational; the cycle has run twice (2026-05-14 original + this 2026-05-15 multi-agent) producing 30 + 11 = 41 items across 5 ships in ~36 hours. Next cycle scheduled 2026-06-15.
- **decision** 02:00 — v9.08 SHIPPED — Showroom polish + Wave 4 composite (MEDIUM-HIGH composite by aggregate; thirty-third ship). VANTA framing: "Right now when I look at all the folders, files, and everything in Polaris, I feel its like a used car. I want you to reorganize everything... brand new Ferrari... fully polished up as if it was show room ready... Then do wave 4." `sanctum/2026-05-15-showroom-reorganization.md` opened+DECIDED-on-arrival; **Position B (surgical polish — no path moves; exhaustive READMEs + master docs + dead-weight removal)** selected per architect recommendation over Position A (hard-reorg under src/+governance/+notes/ — would break v8.20 AoR + every Python import + 3000+ cross-references — calculated as wrong tradeoff: the Ferrari feel is achievable in cabin trim not chassis replacement; Polaris's architecture IS the structure; what's missing is signage; the 10 missing READMEs are the actual smell) and Position C (defer; contradicts directive). **Showroom polish executed:** dead weight removed (8 .DS_Store + .hypothesis/ cache + accumulated __pycache__/ — all already in .gitignore but uncommitted accumulating debris); **10 new READMEs** (assets/, meta/, journal/, scripts/, docs/operator/, docs/reference/, docs/story/, docs/paper/, polaris_swarm/{ants,civitas,legions}/) — every directory now has a one-screen README explaining its role + cross-references + conventions + what-this-isn't section; combined ~700 new lines of doc; **new docs/CONVENTIONS.md** (~400 lines, 16 sections naming all naming + structural conventions: top-level dirs, top-level files, scripts (ai-* / polaris-* / Python helpers), Python package layout, SQL files, test files, Sanctum sessions, journal entries, CHANGELOG entries, node_id format, versioning, doc cross-references, em-dashes, comments, backwards-compat removals, where conventions live); **refreshed docs/reference/SYSTEM-MAP.md** as the architectural centerpiece (~200 lines: at-a-glance project tree showing all 32 directories + four-layers framing (Polaris itself / cognitive substrate / cognitive layer / documentation) + hybrid intelligence pipeline diagram visualizing substrate-vs-lens + constitutional spine listing every load-bearing surface + cross-reference quick map matching CLAUDE.md's table + who-reads-what audience reading orders for 6 audiences); **root README.md refresh** — status line v9.05→v9.08, navigation links to SYSTEM-MAP+CONVENTIONS+MISSION+ROADMAP+CHANGELOG+CLAUDE inline at top of file (was: only the GitHub-style anchor links to in-file sections), all "Polaris in numbers" counts updated to current (33 schema tables (was 25); 60+ HTTP routes (was 53); 540+ structural invariants (was 113); 760+ Python tests (was 470+); 47+ Sanctum sessions (was 17); G1-G33 G-guards (was implicit); 33 commander ants + 8 soldier classes + 6 citizens explicit). **Wave 4 (closing the original 30-item polaris-self-roadmap):** **J2** — new section 9 in `scripts/ai-prime.sh` reads `/tmp/polaris-ai-prime.last` timestamp + computes delta + surfaces ships landed since (greps CHANGELOG headers ≥ last-iso) + HYDRA briefs archived since (mtime check on journal/hydra/) + Sanctums touched since (mtime check on sanctum/2026-*.md) + today's journal decisions recorded; updates the timestamp at exit; first-run prints initialization message; future runs surface the delta. Verified live: first-run printed "First run — no prior session recorded"; subsequent run with simulated 3h-prior printed "Last session: 3.0h ago / Ships since: v9.07, v9.06, v9.05 / HYDRA briefs since: 4 (latest: 2026-05-15-0046.md) / Sanctums touched: 2026-05-15-showroom-reorganization, 2026-05-15-git-or-no-git, 2026-05-15-pheromone-rotation / Today's journal: 3 decision(s) recorded". **Macro re-scan** — new `meta/polaris-self-roadmap-2-2026-05-15.md` (~270 lines) documents post-v9.08 state. **Critical finding: no new constitutional gaps surface**; the project is in the cleanest shape of its life. 6 observations cataloged with explicit dispositions (em-dash blocking promotion candidate, CHANGELOG size trending up — NOT a problem per v8.20 AoR, CLAUDE.md state-map row growth — Sanctum-class candidate for compaction, polaris_web/ churn cluster — true positive of focused work not scope creep, Treasury 60-day eval pending 2026-07-13 per v8.91, soldier silence in dev — expected). 5 explicit "NOT a gap" dismissals (top-level dir count, scripts/ flat layout, doc-domain separation, CHANGELOG size, Python package names — all rationalized). The original polaris-self-roadmap-2026-05-14 is COMPLETE across waves 1-4 (14+8+4+12 = 38 items shipped across v9.05+v9.06+v9.07+v9.08 in ~30 hours). **15 new structural invariants in TestWave4V908 (495→510)** — every showroom invariant pinned (READMEs present, CONVENTIONS sections present, SYSTEM-MAP refreshed, root README current + linked, no .DS_Store outside venv/.git, .gitignore covers caches, ai-prime J2 section present, self-roadmap-II exists + acknowledges Wave 1-4 completion, POLARIS_VERSION at 9.08); **all 563 tests pass** (548 + 15 TestWave4V908). **Constitutional preservation verified end-to-end**: C1 (no AoR rewrite — every prior cross-reference still resolves; new Sanctum + new CHANGELOG + new journal + new sanctum-index entries are all append-only); C10 (no holder PII path touched); G1 (deterministic — pure additions or pure removal of dead weight); **G15 (filesystem-AoR) — 100% preserved verbatim, no path moves**; v8.20 audit-of-record principle preserved verbatim — every Sanctum/journal/CHANGELOG path stays where it was. **Live verification**: ai-prime first-run + simulated-prior-3h-ago second-run both surface the J2 delta correctly; ai-hydra full sweep cleared the Sanctum-index ALERT once showroom Sanctum was added to index (mid-ship); ai-meta HEALTHY; ai-coherence STRUCTURE INTACT; ai-link-check resolves. **Pattern #20 Constitutional Discipline eleventh instance** in the v8.84/v8.87/v8.90/v8.91/v8.94/v8.95/v8.96/v8.97/v9.04/v9.06/v9.07/v9.08 series. `POLARIS_VERSION` bumped 9.07→9.08. **The showroom is open. Every directory has a README. Every naming convention is named. The architectural map is at docs/reference/SYSTEM-MAP.md. Twenty-eight ships in thirty hours; constitutional integrity preserved end-to-end. The macro-to-micro scan → roadmap → wave-by-wave composite-ship pattern is now a documented project rhythm; the cycle has run twice (original 2026-05-14 scan + this 2026-05-15 re-scan); next scheduled re-scan 2026-06-15.**
- **decision** 01:30 — v9.07 SHIPPED — Polaris-self-roadmap Wave 3 composite (4 Sanctum-class HIGH items in one ship; thirty-second ship). VANTA in-chat: "Wave 3 begin" — authorizing the third wave. **The polaris-self-roadmap-2026-05-14 document is now COMPLETE: Wave 1 (14 items) ✅ v9.05; Wave 2 (8 items) ✅ v9.06; Wave 3 (4 items) ✅ v9.07; total 26 items shipped across three ships in 26 hours.** **4 fixes shipped:** **C2** — `sanctum/2026-05-15-git-or-no-git.md` opened + DECIDED-on-arrival; Position A (`git init -b main`, primary-AoR-stays-filesystem) selected per architect recommendation. Three positions filed: A (architect-recommended), B (no-git; rewrite scripts), C (lazy). Position A chosen because the repo already assumed git: `.gitignore` since v8.35, `.github/workflows/ci.yml` since v8.93, `polaris-deploy.sh git pull`, `.pre-commit-config.yaml` v9.06 — for 4 months Polaris was a "git project that isn't git". Filesystem AoR remains canonical (Sanctum/journal/CHANGELOG/treasury-roll discipline unchanged); git is *additive* — a parallel cryptographic chain. **`git init -b main` performed at repo root.** Per Git Safety Protocol ("NEVER commit changes unless the user explicitly asks"), the agent stops at git-init + structural invariant pinning `.git/` existence; operator runs `git add . && git commit -m "v9.07 baseline"` when ready. CI workflow + pre-commit hooks (v9.06/G1) now fully activatable. Pattern #20 tenth instance. **D5-impl** — implementation of the v9.06 D5 Sanctum (which decided Position A: mirror v8.84+v8.87 audit-log archive+purge framework). Migration `2026-05-15-001-pheromone-rotation` (paired up/down): creates `LifecyclePheromoneCheckpoint` table with `archive_sha256` hex CHECK + `cutoff_in_past` CHECK + `rows_purged_nonneg` CHECK; new `reject_pheromone_modification()` trigger function (uses its OWN GUC `polaris.pheromone_purge_in_progress` distinct from audit-log's `polaris.purge_in_progress` so the two carve-out paths cannot cross-contaminate); new `reject_pheromone_checkpoint_modification()` (strictly append-only — NO carve-out at checkpoint layer; G32 parallel to G30); new `uc_pheromone_archive_purge(cutoff, uri, sha256, actor)` procedure (the SINGLE sanctioned DELETE path; G33 parallel to G31; validates cutoff in past + SHA-256 hex + admin role; SET LOCALs the carve-out GUC; DELETEs Pheromone rows older than cutoff; INSERTs checkpoint; COMMIT closes the GUC); `trg_pheromone_append_only` retargeted to the new function; `.down.sql` REFUSES if checkpoints exist (G15: would orphan non-repudiation chain). Operator scripts: `scripts/polaris-pheromone-archive.sh` (~250 lines, EXPORT-ONLY with `--cutoff` (default '30 days ago') / `--out-dir` / `--target=docker-stack` / `--verify-latest` flags; greppable exit codes 0/2/3/4/5; never `CALL`s the procedure); `scripts/polaris-pheromone-purge.sh` (~200 lines, verifies archive SHA-256 against manifest BEFORE issuing CALL + `--archive` / `--cutoff` / `--actor-user-id` / `--dry-run` + 6 exit codes including EXIT_CUTOFF_MISMATCH=6). MISSION.md G-guards updated `G1-G29` → `G1-G33` with G32+G33 named explicitly. OPERATIONS.md § "Pheromone archive + purge (v9.07 / D5-impl)" added with two-step workflow + adversarial guarantees + cadence table updated (3 new rows for archive + verify + purge). **End-to-end drill verified live**: 2 force-inserted old rows (test_old_ant @ 2025-01-01 + 2025-02-01) → `CALL uc_pheromone_archive_purge('2026-01-01'::timestamptz, 'file:///tmp/...', '<sha>', 1)` → 2 rows deleted, checkpoint id=1 written; after COMMIT, raw `DELETE FROM Pheromone WHERE pheromone_id=...` REJECTED with `insufficient_privilege` (carve-out evaporated as designed); raw `UPDATE Pheromone` REJECTED (no carve-out for UPDATE); raw `DELETE FROM LifecyclePheromoneCheckpoint` REJECTED (G32). **J1** — new `scripts/ai-dashboard.sh` (~300 lines) composes 7 sections into single screen: mission state (constraints + done-list) + top moves (ai-propose top-3) + latest HYDRA brief (count + age + Stale/Dead/Fresh per H1 thresholds) + treasury health (with v9.05/A1 cutover-aware soldier check that distinguishes 19 historical pre-v9.05 entries from 0 post-v9.05 violations) + open Sanctums + recent ships (top-3 from CHANGELOG) + swarm substrate (commander + soldier counts + classes-active-24h via live SQL). Modes: default (one render) + `--quick` (skip slow checks) + `--json` (machine-readable) + `--watch [N]` (re-render every N seconds; default 30). Each section is a 4-8 line block; total fits 80x40. **J4** — new `meta/treasury-60d-sim-review-2026-05-15.md` (~140 lines) reviews the v8.91 Sanctum's 60-day commitment in light of v9.05's A1 (F5 soldier-exemption restored) + B1+B2 (ant venv-blindness fixed). The constituency changed materially: pre-v9.05 there were 10 drift-class ants eligible for Eques; post-v9.05 only ~4 are non-soldier non-STEADY_STATE. Two paths considered: A (keep original 2026-07-13 window; re-baseline metric if needed via Wave 4 / Treasury-Rebalance-II Sanctum) vs B (reset to 2026-07-14 + 60d). **Architect-recommended Path A** because (1) the +10/-1 ratio is unchanged in v9.05; the mechanism is the same; only the population shifted; resetting the window punishes the v8.91 decision for being right; (2) re-baselining the metric is cheaper than re-baselining the window. Recommended Wave 4 actions: re-run treasury sim 2026-05-22 (1 week post v9.05); re-run macro-to-micro scan 2026-06-15 (~30d post v9.04 scan); formal v8.91 60-day evaluation 2026-07-13. J4 closes with the document filed; the substantive 60-day evaluation remains scheduled. **22 new structural invariants in TestWave3V907** (473→495); **all 547+ tests pass** (structural + hydra-revamp + property). **Constitutional preservation verified end-to-end**: C1 (G32+G33 carve-out exactly parallel to v8.87 G30+G31 — raw DELETE on Pheromone rejected outside procedure; UPDATE always rejected; checkpoint strictly append-only with no carve-out at that layer); C10 (no holder PII path touched); G1 (deterministic given cutoff+archive+actor); G3 (graceful exit codes; trigger raises `insufficient_privilege` with named ERRCODE); G6 (rotation framework lives in polaris_sql/ + scripts/; no cross-tier imports); **G32 (NEW): LifecyclePheromoneCheckpoint strictly append-only; G33 (NEW): uc_pheromone_archive_purge is the only sanctioned DELETE path on Pheromone**; F5 (J4 review preserves v8.91 60-day commitment + recommends Path A — the +10/-1 ratio is unchanged). **Pattern #20 Constitutional Discipline tenth instance** (C2 + D5-impl both Sanctum-decided in this composite + v9.06 D5 opening) + **Pattern #19 Clarity** (J1 + J4 both name something previously implicit: "give me one screen of state" and "what does the v8.91 sim mean now that the cohort changed"). `POLARIS_VERSION` bumped 9.06 → 9.07. **The polaris-self-roadmap-2026-05-14 document is now COMPLETE.** **30 items in 3 composite ships in ~26 hours; Pattern #20 cycled 11 times this week (v8.84/v8.87/v8.90/v8.91/v8.94/v8.95/v8.96/v8.97/v9.04/v9.06/v9.07)**; constitutional integrity preserved end-to-end. **The macro-to-micro scan → roadmap → wave-by-wave composite-ship pattern is now a documented project rhythm**: scan surfaces ALL gaps; roadmap categorizes by autonomous/Sanctum/operator-decision; waves ship in priority order; structural invariants pin each fix; the roadmap document itself becomes audit-of-record; Wave 4 ongoing observational re-scan candidates surface naturally for next cycle.
- **decision** 00:50 — v9.06 SHIPPED — Polaris-self-roadmap Wave 2 composite (8 MEDIUM items in one ship; thirty-first ship). VANTA in-chat: "wave 2 proceed" — authorizing the second wave. Composite single ship as v9.06 mirrors v9.05 Wave 1 shape. **8 fixes shipped:** **H1 (the most architecturally interesting move)** — cognitive_watcher gains channel 6 reading `journal/hydra/<date>-<HHMM>.md` freshness via `_check_hydra_brief_freshness()`; `HYDRA_BRIEF_STALE_DAYS = 14.0` → drift, `HYDRA_BRIEF_DEAD_DAYS = 30.0` → alert; surfaces empty-archive as info, fresh as healthy (no finding), stale as drift, dead as alert. **The lens now watches its own observation output**; pre-v9.06 if HYDRA's brief-archive stopped accumulating no other watcher would notice. Channel is filesystem-only (no DB, no LLM) so it works offline. **C1** — `ai-architect.sh --reflect` gains `do_reflect_hydra_briefs()` surfacing HYDRA brief count + latest filename + age + Stale/Dead/Fresh classification (matches H1 thresholds; consistent vocabulary across the cognitive layer). Pre-v9.06 the Architect's reflect-mode read `journal/*-architect.md` only; v9.04's `journal/hydra/` was invisible. **D5** — `sanctum/2026-05-15-pheromone-rotation.md` opened + DECIDED + CLOSED in same surface (heavy-production posture per v8.31 §III.6). Position A (mirror v8.84+v8.87 audit-log archive+purge framework) selected per architect recommendation. Implementation deferred to Wave 3 as separate ship surface (mirrors v8.84 → v8.87 two-step pattern). 4 §IV resolutions per architect-recommended defaults: 30-day cutoff, quarterly archive + yearly purge cadence, NO per-tier retention (same cutoff for commander + soldier), strict acceptance = byte-identical archive output. **Pattern #20 ninth instance** in the v8.84/v8.87/v8.90/v8.91/v8.94/v8.95/v8.96/v8.97/v9.04/v9.06 series. **C5** — new `polaris_web/__version__.py` exports `__version__: str = "9.06"` + `POLARIS_VERSION` alias; app.py imports from it (was: `POLARIS_VERSION = '9.05'` literal); structural invariant `test_c5_app_py_imports_from_version_module` forbids re-introducing the literal. Future surfaces (CLI, Dockerfile labels, OpenAPI docs) will all import from one place. **E2** — new `polaris_web/test_hydra_property.py` (~280 lines) with 9 Hypothesis property tests covering CorrelationEngine + ActionQueue: determinism (G1), sort invariants (-score then key for correlations; -score then risk_order then title for actions), top_n bounds, action invariants (score ≥ 0; risk_class ∈ {LOW, MEDIUM, HIGH}; effort_estimate ∈ {one-shot, one-day, multi-ship}; source_kind ∈ {finding, correlation}), single-watcher edge case (Strategy 1 needs ≥2 distinct watchers), info-singleton skip. Hypothesis count 10 → 19. Generative testing covers what unit tests sample-test. Required `pip install 'hypothesis>=6.0,<7.0'` in the dev venv (already in requirements.txt from v9.05 / C3). **G1** — new `.pre-commit-config.yaml` at repo root with 6 local hooks (ai-link-check ~2s + ai-meta ~3s + ai-coherence ~5s + structural-invariants ~25s + g28-no-sensitive-env-in-prod-compose <1s + em-dash-warn <1s); local-hooks shape (not third-party `repos:`) so Polaris works whether or not git-initialized (the C2 / Wave 3 question). OPERATIONS.md §"Pre-commit hooks (v9.06)" added with install + manual run + CI-as-safety-net + why-local-hooks rationale. **I1** — `DEVNOTES/hydra-pheromone-integration.md` extended with "node_id format convention" section enumerating 7 canonical domains: `route:` (security/performance/route_pinger) / `schema:` (schema/db_table_size) / `infra:` (security/log_tail/disk_usage) / `cognitive:` (cognitive/sanctum_freshness/hydra_brief) / `swarm:` (ant_colony) / `civitas:` (civitas/treasury) / `mission:` (mission/done_list_arithmetic) + 1 reserved (`build:` for ant_build_freshness) + 2 historical (`file:` and `module:`). Explains why colon convention matters: without it, `_domain_prefix_of()` silently degrades Strategy 2 correlations to no-op. **J3** — new `meta/claude-90s.md` (~110 lines) extracts CLAUDE.md's operative 30 lines: what Polaris is + first 90 seconds (3 commands) + VANTA's expectations + 3 nevers (skip Sanctum / violate C1-C10 / silently delete) + 3 always (use cognitive layer / update AoR / verify before done) + where-to-look table + current state + the-one-rule. ai-prime.sh refreshed to point at it. The 605-line CLAUDE.md remains as deep reference. **19 new structural invariants in `TestWave2V906` (454→473)**; **9 new Hypothesis property tests** (in `test_hydra_property.py`); all 502+ tests pass. **Constitutional preservation verified end-to-end**: C1 (`__version__.py` is new code, not AoR rewrite; pre-commit hooks preserve drift detection; node_id docs are pure addition); C10 (no holder PII path touched); **G1 (Hypothesis property tests EXPAND determinism guarantees for CorrelationEngine + ActionQueue)**; G3 (H1 channel is filesystem-only — works offline); F5 (D5 Sanctum preserves v9.03 soldier-exempt claim by treating Pheromone rotation as separate question). **Live drill verified**: `bash scripts/ai-architect.sh --reflect` surfaces 3 HYDRA briefs with "fresh" classification + "HYDRA's compute_delta reads cross-run; briefs are mutually comparable" hint; `bash scripts/ai-hydra.sh --watcher cognitive` evidence_summary contains `hydra_brief_archive_status=fresh / hydra_brief_count=3 / hydra_brief_age_days=<small> / hydra_brief_latest=<latest>.md`; `from polaris_web.__version__ import POLARIS_VERSION` returns '9.06'; `app.POLARIS_VERSION` returns '9.06' via import not literal; 9/9 Hypothesis property tests pass. **Pattern #20 ninth instance** (D5 Sanctum) + **Pattern #19 Clarity** (node_id format docs + claude-90s.md + brief-archive unification all name something previously implicit). `POLARIS_VERSION` bumped 9.05 → 9.06 (via the new __version__.py module). **Wave 2 of the polaris-self-roadmap closes with 8/8 items shipped.** Wave 3 (4 Sanctum-class HIGH items: git-or-no-git decision (C2), Pheromone rotation framework implementation (D5-impl), ai-dashboard.sh (J1), Treasury 60-day sim review (J4)) remains awaiting VANTA decisions.
- **decision** 00:30 — v9.05 SHIPPED — Polaris-self-roadmap Wave 1 composite (14 autonomous-eligible items in one ship; LOW composite risk; thirtieth-plus ship). VANTA in-chat 2026-05-14: "Wave 1 begin" — authorizing the composite ship of all 14 autonomous-eligible items from the macro-to-micro scan's [`meta/polaris-self-roadmap-2026-05-14.md`](../meta/polaris-self-roadmap-2026-05-14.md). **Same shape as v8.93 Phase 2 closing-pass + v9.02 dangling-thread closure**: surface gaps via macro-to-micro scan → fix bundle in one ship → preserve audit trail via the roadmap document → pin via structural invariants. **14 items shipped end-to-end:** **A1 (constitutional bug fix)** — v9.03 Sanctum §VI claimed soldiers were F5-EXEMPT from Treasury accrual ("disposable invariant") but `compute_rewards()` only checked the `STEADY_STATE_ANTS: frozenset` allowlist; soldiers weren't in it; live evidence was 21 `soldier_*` events accrued in `treasury-roll.json` since v9.03 ship date. Fix: new `is_treasury_exempt(ant_name) -> bool` predicate routes both the allowlist AND `ant_name.startswith("soldier_")`; `compute_rewards()` uses it; `SOLDIER_NAME_PREFIX = "soldier_"` constant added. Per G15 the 21 historical events stay (audit-of-record); new soldier accruals are now structurally impossible. `_audit` field appended to treasury-roll.json documenting the cutover. **A2** — `bash scripts/ai-test-counts.sh --update` ran; MISSION.md item 7 went from "445 across 53 TestCase" to "763 across 132 TestCase" (current reality). **B1+B2 (substrate hygiene; biggest surface)** — pre-v9.05 the macro-to-micro scan caught 708/725 (97.7%) of `ant_test_gap` deposits + 96/102 (94%) of `ant_todo_debt` deposits as venv/site-packages noise; 11 ants/legions had no skip-pattern for venv. New `polaris_swarm/scan_filters.py` (~140 lines) exports `is_polaris_source(path) -> bool` keyed on `SKIP_DIR_NAMES` (venv/.venv/site-packages/__pycache__/.git/target/node_modules/.pytest_cache/.mypy_cache/.ruff_cache/dist/build/coverage_html/htmlcov) + `SKIP_FILE_NAMES` (__init__.py/conftest.py/.DS_Store) + `filter_paths()` + `is_polaris_module()`. 8 ants refactored: ant_test_gap, ant_todo_debt, ant_recent_churn, ant_changelog_gap, ant_build_freshness, ant_brain_map_freshness, ant_dependency_in_use, ant_unbumped_version. **Live verification: ant_test_gap dropped from 725 deposits → 20 deposits (0% noise, was 97.7%); ant_todo_debt 102 → 5; ant_brain_map_freshness many → 0** (the most-recent venv mtime was masking the real project mtime). **C3** — `polaris_web/requirements.txt` (NEW) captured via `pip freeze` from the dev venv; 19 packages with grouped sections (Flask web stack / WSGI / Postgres / WebAuthn / test-only / Prometheus); CI installs via `-r polaris_web/requirements.txt` (was: inline list of 5 packages with loose specs); both Dockerfiles `COPY requirements.txt /tmp/requirements.txt && pip install -r /tmp/requirements.txt`. **C4** — `ai-help.sh` surfaces key flags inline via new `flags_for()` awk helper (scans the doc-comment block for `--<word>` patterns; print_group emits a `flags: ...` sub-line; capped at 6 flags per script for density). **D1** — brief-archive collision detection: `archive_brief()` checks `path.exists()` + appends `-1`, `-2`, … up to 999 + defensive RuntimeError beyond; pre-v9.05 same-minute saves silently overwrote. **D2** — centralized PheromoneReader window defaults: new `WINDOW_FAST = 6.0` + `WINDOW_SLOW = 24.0` constants; security/performance use WINDOW_FAST; schema/cognitive use WINDOW_SLOW; ant_colony aliases `PHEROMONE_WINDOW_HOURS = WINDOW_FAST`. **D4** — `--diff`-without-`--save` is pure in-memory: new `compute_delta_in_memory()` extracts title sets directly from in-memory `reports + actions` (no disk roundtrip); `Hydra.speak_full()`'s diff-without-save branch uses it; new `_relpath_or_abs()` helper handles prior briefs outside repo root. **E1** — 5 new test classes in `polaris_web/test_hydra_revamp.py`: TestSpeakFullDiffInMemory (asserts no temp file written when diff-without-save) + TestBriefArchiveCollision (3 same-minute saves → 3 distinct files) + TestFullSaveDiffCycle (full two-save → compute_delta round-trip) + TestF5SoldierExemption (predicate works for KNOWN_SOLDIER_CLASSES_V9_03 + arbitrary future soldiers + STEADY_STATE_ANTS members + drift-class ants are not exempt) + TestScanFilters (skips venv/site-packages/target/etc., keeps real Polaris paths). Total unit tests 33 → 44. **F1+F2+F3 (doc updates)** — CLAUDE.md intro now names the v9.x era + substrate-vs-lens vocabulary (was "v1 through v8.x"); README status line refreshed v8.61→v9.05 + names hybrid intelligence model; CLAUDE.md "Where does X live?" gains 7 new rows (journal/hydra/, hydra-pheromone-integration.md, swarm-tier-vocabulary.md, SYSTEM-MAP.md, PRINCIPLES.md, requirements.txt, scan_filters.py). **I2** — HYDRA `--deterministic` CLI flag: `force_deterministic=False` kwarg on `Hydra.speak()` + `speak_full()`; new `mode_reason='forced_deterministic'` distinguishes from `no_anthropic_key` and `llm_error:<type>`; ai-hydra.sh wrapper documents it. **28 new structural invariants in `TestWave1V905` (426→454)**; **all 44 hydra-revamp unit tests pass**. Plus updates to TestAntColonyWatcherGracefulFailure for the v9.05 reality. **Constitutional preservation verified end-to-end**: C1 (treasury-roll.json keeps the 19 historical soldier events per G15; brief_archive collision-detection never overwrites; scan_filters is read-only); C10 (only metadata flows; no holder PII path touched); G1/G3/G16 (every change deterministic + read-only + pure-function); **F5 (the v9.03 Sanctum's soldier-exempt claim is now structurally enforced — was previously only narratively asserted; the `is_treasury_exempt()` predicate makes the constitutional invariant mechanical)**. **The macro-to-micro scan → roadmap → composite-ship pattern is now a documented project rhythm**: scan surfaces ALL gaps; roadmap categorizes by autonomous/Sanctum/operator-decision; Wave 1 ships autonomously in one composite; future Wave 2/3 items reference the same roadmap. `POLARIS_VERSION` bumped 9.04→9.05. **Wave 1 of the polaris-self-roadmap closes with 14/14 items shipped.** Wave 2 (8 MEDIUM-risk items: brief-archive Architect+HYDRA unification, canonical POLARIS_VERSION source, Pheromone rotation Sanctum, Hypothesis tests for v9.04 modules, pre-commit hooks, cognitive_watcher channel for journal/hydra/ freshness ["the lens watching itself" — the most interesting move], node_id format docs+lint, 90-second Claude onboarding doc) and Wave 3 (4 Sanctum-class HIGH: git-or-no-git, Pheromone rotation framework, ai-dashboard.sh, Treasury 60-day sim review) remain in the roadmap awaiting VANTA decisions.
