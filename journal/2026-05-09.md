# Journal — 2026-05-09

Episodic memory for this date. Each session opens with "## SESSION
HH:MM" and closes with "## END HH:MM". Decisions and learnings are
flat-list under their session.

## SESSION 03:43 — R7-3 cursor pagination on /tokens and /verifications — eliminate OFFSET 13.6s slowness

- **decision** 04:00 — Used composite cursor (ts~event_id) for /verifications instead of single column event_id — schema permits same-timestamp events, so single-key would drop or duplicate boundary rows. Row-value compare rides idx_verificationevent_time_id in one shot.
- **learning** 04:00 — Werkzeug test_client does NOT decode HTML entities. Pager hrefs render &amp; (Jinja autoescape); real browsers decode but client.get takes the URL verbatim, so &amp;cursor= breaks param parsing. Tests must html.unescape() before resending. Documented in patterns/add-list-page-pagination.md.
- **learning** 04:00 — 00_load_all.sql includes 12_v7_constraints.sql which inflates row counts beyond pristine 04_data (7 tokens / 9 events vs 5/8). reload_sample_data only re-runs a subset, so tests see the pristine count. Don't trust post-bootstrap counts as fixture truth.
- **decision** 04:02 — v7.4 R7-3 cursor pagination shipped. 11 new tests pass, full suite 135/135. Composite cursor for verifications, single-int for tokens. Pager macro bimodal. Mission item 10 advanced. Side fixes: portable reload_sample_data on macOS, F05 hardcoded path, ai-status C2 grep aligned with schema CHECK, MISSION.md C2 reference corrected.
- **decision** 04:17 — v7.5 R8-2 multi-process rate limiter shipped. Two backends (InMemoryRateLimiter, RedisRateLimiter) implementing _BaseRateLimiter contract. Redis uses Lua sliding-window for atomicity. Contract test mixin runs identical assertions against both. Mission item 12 advanced. Total 161/161 green.
- **learning** 04:17 — OWASP fail-secure for rate limiting = fail CLOSED. Fail-open silently bypasses the limiter under load. Documented in DEVNOTES/rate-limiter.md.
- **decision** 04:37 — v2 mission opened 2026-05-09. Arc: D + A (substrate-level demonstrations + PDF §9 open problems). 12 new done-list items M2-1..M2-12 in MISSION.md. v1 archived inline as historical record (12/15 done, 3 deferred). Roadmap: R10-* substrate, R11-* open problems. Considered arcs B (adversarial hardening) and C (Polaris-as-platform) documented in meta/missions-considered.md for future. ai-status and ai-propose updated to recognize R10/R11 + skip deferred items.
- **decision** 04:54 — v8 shipped: M2-4 GenomicAnchor + M2-12 redaction proof. v2 progress 0/12 → 2/12. GenomicAnchor uses three layered CHECK constraints (hex format / algorithm-length / non-genomic-alphabet) to make Appendix F.1's privacy invariant schema-enforceable; 11 tests cover each constraint's failure mode. Redaction proof in meta/redaction-proof.md instantiates an adversary model with 5 enumerated side-channels; test_redaction_property.py has 3 adversary classes and 6 tests including 2 counterexample tests that confirm CE-1/CE-2 succeed (documented limitations). Total 178 tests, all 10 hard constraints green.
- **learning** 04:54 — When testing a privacy claim, also test the COUNTEREXAMPLES — the conditions under which the claim breaks. A claim that hides its failure modes is weaker than one that names them. test_redaction_property has 4 'success' tests confirming the claim, and 2 'failure' tests confirming the documented limitations. Both directions matter.
- **decision** 05:03 — v8.1 R10-3 substrate-dependency manifest shipped. 23 primitives across 7 layers in DEVNOTES/substrate.md (prose) + SystemDependency view in 13_substrate.sql (queryable). Drift-detection test asserts prose/SQL agreement. Operationalizes Appendix E. v2 mission 2/12 → 3/12. Tests 178 → 184 all green.
- **decision** 05:35 — v8.2 Atlas visual pass — V1 (chip active state, HUD type scale, dead-CSS sweep, toolbar wrap fix, cache-busters) + V2 (reticle visibility, new-arrival pulse). Discovered enter.merge(sel).classed() didn't stick; switched to post-render selection.each + native classList.add. seenNodeIds tracking lights up only first-appearance ids, not filter-rotation repeats. 184 tests green.
- **learning** 05:35 — d3 selection.classed() on enter.merge(sel) is unreliable in this codebase — class never lands. Use post-render selectAll() + each() + native classList.add() instead. Took ~30 minutes of cache-bust debugging to figure out it was a code path issue, not a cache issue.
- **decision** 06:01 — v8.3 Atlas A+C scaling pass shipped. Temporal lens (1h/24h/7d/30d/all + 60-bucket histogram strip) + operational filter primitives (anomalies/FULL/PQ multi-select + 7-context multi-select picker) cut visible event set ~100x for the 1M+ scale concern. Two-band toolbar splits view/modifier/context (row 1) from window/histogram/LIVE (row 2). New /api/atlas/timeline endpoint, six SQL functions extended with optional filter params, all input validated against whitelists. 196 tests green (+12 filter tests).
- **learning** 06:01 — Postgres TIMESTAMP-without-zone columns are local wall-clock. datetime.utcnow() in Python silently shifts the boundary by the server's TZ offset. Use datetime.now() when comparing against TIMESTAMP-no-TZ columns. Caught during smoke test where window=1h returned 0 events for a 30-min-old insertion.
- **learning** 06:01 — Postgres function overloading silently keeps both old and new signatures coexisting. When extending an existing atlas SQL function with new optional DEFAULT NULL params, must DROP FUNCTION IF EXISTS with the OLD signature first; CREATE OR REPLACE only matches identical signatures.
- **decision** 06:14 — v8.4 repo reorg: 9 reference docs moved from root to docs/. Root is now 6 cognitive/conventional .md files (BACKLOG, CHANGELOG, CLAUDE, MISSION, README, ROADMAP). 23 cross-refs updated by Python pass with lookbehind regex; 8 path-prefixed forms caught in second pass. Full suite 196/196 still green. ai-status + ai-propose still work. New docs/README.md indexes everything by audience.
- **learning** 06:14 — When bulk-renaming paths in Markdown/code, use a regex with negative lookbehind (?<![/\\w-]) to avoid re-prefixing already-correct refs. Sed alone can't do lookbehinds; Python's re module can. Two passes — first for bare names, second for path-prefixed forms ($ROOT/X.md, ../X.md, ../../X.md) — caught everything.
- **decision** 22:18 — v8.5 cognitive architecture v2: three meta-tools shipped — ai-prime.sh (single-command session primer, replaces 3-5 min onboarding with 30s), ai-test.sh (one-shot test runner, eliminates 25s/run env-var ceremony), ai-link-check.sh (proactive cross-ref validator, safety net for future reorgs). Plus stale-numbers cleanup (MISSION test count 134 -> 196), proposals/ index, meta/cognitive-architecture-v2.md documenting what changed and what's deliberately deferred.
- **learning** 22:18 — Backticks inside the regex of a heredoc'd python script confuse bash's parser, even when the heredoc is single-quoted. The $( ... ) command substitution still scans for nested backtick command-substitution. Solution: drop the backtick from character classes that don't need it. Cost me one syntax-error round trip on ai-link-check.sh.
- **decision** 22:36 — v8.6 full-spectrum cognitive pass: 6 new meta-scripts (ai-help, ai-mission, ai-test-counts, ai-impact, ai-cache-bust, ai-snapshot, ai-done) + 5 new known-gotchas entries + meta/cognitive-architecture-v3.md. Caught two drifts on first run: MISSION.md test count 134 -> 203 Python and 39 -> 126 SQL. ai-done.sh is the new pre-ship gate; a single command rolls up status + link-check + cache-bust + test-counts + journal + CHANGELOG + debug-code + bare-refs.
- **decision** 22:41 — v8.7 honest cleanup. Removed: polaris_complete.sql (111KB orphan, was a single-file bundle dup with 08_tests.sql; removing it also fixed ai-test-counts double-counting from 126 -> 63 actual SQL tests), .DS_Store, polaris_web/__pycache__. Did NOT remove the 'sacred' layer (test_sacred_structure.py, sacred-numerology.json, ai-archetype, ai-resonance) — audit revealed it's load-bearing per MISSION.md Sefirotic mapping + 18 passing Python tests + cross-script references.
- **learning** 22:41 — Before pruning, ALWAYS audit references first. What looks decorative often turns out to be load-bearing. The 'sacred' layer in this codebase has a Python test suite, JSON config, MISSION.md references, and is cross-linked through propose/resonance/archetype scripts. Almost removed it; would have collapsed the architectural identity.
