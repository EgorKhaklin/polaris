# Journal — 2026-05-11

Episodic memory for this date. Each session opens with "## SESSION
HH:MM" and closes with "## END HH:MM". Decisions and learnings are
flat-list under their session.
- **decision** 00:02 — v8.9 — built ai-meta.sh, filled the reserved meta-slot with CM, added --compose to ai-pattern.sh, cross-pollinated ai-propose.sh with lattice polarity complements. Discovered all three gaps by USING the v8.8 tools first (ai-pattern.sh failed on 'self-improvement' query; ai-pattern.sh missed multi-pattern situations; meta-slot was still in limbo). 28/28 structural tests pass (up from 20). ai-meta.sh on first run revealed only 3/22 patterns warm — pattern catalog isn't being invoked in journals as often as it could be.
- **learning** 00:02 — When asked to improve the cognitive architecture, RUN it first. Don't design from imagination. The tools find gaps that introspection misses: ai-pattern.sh's score=0 result for the self-improvement query was the clearest possible signal that the 22-pattern catalog has a meta-cognition gap. Running > thinking, when there are tools to run.
- **decision** 00:18 — v8.10 — added game-theoretic adversary lens. ai-adversary.sh walks each constraint as a game: defender claim, attacker's optimal response, equilibrium goal, second-best attack, cost, mechanism-design note. Annotated 22-pattern catalog with game-theoretic types. Added 7th structural framework (Adversarial framing). 35/35 structural tests pass.
- **decision** 00:18 — v8.10 cleanup: removed proposals/R7-3 and R8-2 (shipped), .DS_Store, __pycache__/. Reorganized ai-help.sh from 4 groups (one with 11 scripts) into 6 groups (each <=5 scripts). Audited 5 'maybe-dead' candidates with ai-impact.sh — kept all 5 (genuinely load-bearing). The compose pattern Removal+Composition+Branchpoint correctly predicted the work shape.
- **learning** 00:18 — Use the architecture to evaluate the architecture. Running ai-pattern --compose on the task BEFORE planning surfaced the dominant patterns. ai-impact.sh per removal candidate prevented v8.7's 'too conservative' failure mode. ai-meta.sh caught ai-adversary.sh's CLAUDE.md orphan immediately. The cognitive+meta-cognitive loop catches what either alone misses.
- **decision** 00:32 — v8.11 — shipped M2-5 / R10-5 (QuantumObserverBinding scaffold). Used cognitive layer: ai-prime surfaced R10-5 as top LOW-risk; ai-pattern --compose returned Composition+Branchpoint with shadow 'tool-fixation/over-engineering' — exactly the right guardrail for a scaffold deliverable. Added 9 tests in QuantumObserverBindingTests, full suite at 205/205. test_prose_and_sql_forms_agree caught DEVNOTES/substrate.md drift immediately when I added the SystemDependency row.
- **learning** 00:32 — The cognitive layer's shadow prediction matches deliverable type. Scaffold tasks predicted Composition's shadow (tool-fixation). v8.7 cleanup's shadow was Sentimental Keep. v8.10 dev was Removal-dominant. The shadow is a real predictive signal, not decoration — it tells you which failure mode applies to the current work shape.
- **learning** 00:36 — Note added to BACKLOG: Cortana/Jarvis-style operator-AI assistant for Polaris. Speculative, not for auto-propose. Captured the C5/C2/C6/C10/CM constraint considerations and game-theoretic structure (Principal-agent, prompt-injection vector, third-party-LLM exfiltration risk). The architecturally-correct shape is read-only synthesis layer over already-redacted Polaris views — never raw data, never state-changing.
- **decision** 00:44 — Updated all 7 README files (root, polaris_web, polaris_sql, polaris_cli, patterns, docs, proposals) to reflect v8.x current state: 14 tables (was 12-13), 77 sample rows (was 73), 228 Python tests across 39 classes (was 92 across 12), 63 SQL self-tests across 3 files (was 36 across 1), 7 stored procedures (was 4), additional schema files 11_atlas/12_v7_constraints/13_substrate, atlas-globe.js + two-band toolbar + filter API, R8-2 multi-process rate limiter, M2-5 QuantumObserverBinding scaffold, decomposition-targets/schema-change/security-fix patterns added to index. Used ai-pattern compose first: Investigation pattern, shadow = bus-factor — outdated READMEs ARE bus-factor-1.
- **decision** 00:50 — Comprehensive doc sweep: CLAUDE.md version timeline (v1..v8.11) + current-size summary (14 tables, 228 Python tests, 64 SQL tests, 26 ai-scripts), docs/DATA-MODEL.md rewritten (12 → 14 tables, 4 → 6 functional groups, added Records&Substrate + Junctions sections, added atlas_timeline + v8.3 filter params, removed phantom 'BiometricEnrollment' table that never existed), docs/INSTALL.md test counts refreshed (101 → 228, 36 → 64 SQL). Used ai-pattern --compose to plan but task scored 0 (meta-organizational, not in 22-pattern catalog); random fallback was Authority (brittle authority = outdated docs) — exact right framing.
- **learning** 00:50 — Discovered docs/DATA-MODEL.md listed a phantom 'BiometricEnrollment' table that never existed in 01_schema.sql. The list-driven docs accumulated drift the structural tests didn't catch (those test SCHEMA invariants, not DOC accuracy). New gap: no executable test that says 'docs/DATA-MODEL.md mentions all and only tables that exist'. Worth a future cognitive-layer test.
- **decision** 00:55 — v8.12 P4 shipped — TestDocSchemaCorrespondence in test_structural_invariants.py. Three assertions: schema-tables extractor not broken (sanity), every CREATE TABLE documented in DATA-MODEL.md (forward), no phantom doc-only tables (reverse). The third catches the v8.11 BiometricEnrollment failure mode. Verified by simulating phantom injection — test fires correctly. 38/38 structural pass (was 35). 205/205 integration suite pass.
- **learning** 00:55 — The 'found drift → wrote test' loop is the durable pattern. v8.11 found BiometricEnrollment listed-but-nonexistent. v8.12 makes that class of drift impossible to ship again. The journal learning was the bridge: observed in v8.11, ai-journal.sh logged it with 'consider promoting to DEVNOTES if recurrent', and the next session promoted it directly to an executable check. Pattern: drift → journal → test. Single-step promotion is fine when the gap is concrete.
- **decision** 01:11 — v8.13 — Polaris Architect shipped. meta/architect.md (persona spec, 200 lines) + scripts/ai-architect.sh (brief generator, 500 lines) + 5 new tests. Three modes: full, --cron, --reflect. Brief structure: six sections (State/Outlook/Drift/Threats/Suggestions/Self-monitoring). Each suggestion gets arch-YYYY-MM-DD-NNN ID for cross-brief tracking. Game-type lookup annotates roadmap recommendations. The em-dash voice rule (DEVNOTES/style.md) is enforced as a test that caught real violations on first run — the persona enforces itself.
- **learning** 01:11 — Persona specs become load-bearing when tested. meta/architect.md said 'no em-dashes in own prose.' Without a test, that's aspiration. With test_architect_cites_no_em_dashes_in_own_strings, it's an invariant. Three em-dashes caught on first run, fixed before ship. Same pattern as v8.12 (BiometricEnrollment phantom → executable test): every voice/style rule that matters should be testable, or it'll drift.
- **decision** 01:16 — Wrote 3 proposals to proposals/: R10-2-did-anchoring (213 lines), R11-2-catastrophic-loss-recovery (271 lines), R11-1-multisig-transitional (261 lines). Status: PROPOSED, awaiting VANTA approval. References arch-2026-05-11-001 from first Architect brief. Each includes: Problem, Why MEDIUM, Game-theoretic structure (pulled from ai-adversary.sh walks), Recommended approach, Implementation sketch with full SQL/Python, Predicted blast radius, Acceptance criteria, What this is NOT, What this needs from you. ai-link-check: 15/15 (was 12; +3 from new proposal cross-references).
- **decision** 02:20 — BACKLOG note upgraded: Operator-AI assistant idea now specifies OpenJarvis (https://github.com/open-jarvis/OpenJarvis) as the recommended substrate. Apache 2.0, Ollama-based local inference, Tauri desktop (outside Polaris web stack — C5 unaffected), skill system matches the 'thin NL layer over structured data' shape from the original note. Implementation sketch upgraded from 4 bullets to 6 numbered steps with concrete file paths: read-only polaris_assistant role + assistant schema views + skill bundle + AssistantAuditLog + code-execution disabled + threat-model extension. Total effort ~4-5 sessions when promoted. Status remains: not now, awaiting explicit user direction.
- **decision** 02:32 — v8.14 — HD Sci-Fi skin shipped. polaris_web/static/polaris-scifi.css (~700 lines) loaded after polaris.css, opt-in via body.theme-scifi. No external dependencies (C5 preserved). All 205 integration tests pass. Visual verified via preview on /login, /, /atlas, /uc7 — all four pages render correctly with cyber-blue + glass + hex-grid + glow effects. Removed pattern: 'premature closure' (Closure shadow) caught me missing form.standard on iteration 1; fixed in iteration 2.
- **learning** 02:32 — Layered CSS skins are the right pattern for 'complete redesign' on a stable codebase. Adding polaris-scifi.css as additive layer (loaded AFTER polaris.css, opt-in via body class) gave a complete visual transformation with zero risk to the 205 integration tests. Reversion is removing one <link> tag. Worth remembering as the default for visual overhauls — full repaints of polaris.css would have risked breaking existing class assertions.
- **decision** 02:50 — v8.14 iteration 3: closed visual-feedback round. Fixed lingering white surfaces on: SQL example queries, dashboard atlas-panel/atlas-panel-wide sections (PQ migration + verif activity), token detail subsections, UC-7 datetime-local inputs (via color-scheme: dark), .disclosure-note privacy callout, inline <code> tags. Color-coded by type: agency types (5 — FEDERAL=gold, STATE=cyan, PRIVATE=green, COUNTY=violet, MUNICIPAL=pink), algorithm types (PQ=green, classical=red), disclosure levels (FULL=gold, SELECTIVE=cyan, ZK=green), audit events by type. Brightened .stat-card .value to white-with-stacked-glow. Template edit on agencies_list.html: inline style → class hook. Closure shadow caught: I almost shipped after iteration 1.
- **decision** 03:03 — v8.14 iteration 4: login page made dramatic (glowing POLARIS title, AWAITING CREDENTIALS gold-pulse indicator, gold corner bracket, bold gold Sign In button). Color-coded disclosure-level cards as traffic-light: ZERO_KNOWLEDGE=neon-green (safe), SELECTIVE=gold (curated), FULL=neon-amber (elevated). Refined audit events into 5 distinct tones (cyan/green/amber/steel/red) for ISSUED/ACTIVATED/DORMANT/EXPIRED/LOST-or-REVOKED. Token Succession Lineage chain styled: cyan tokens + gold current + animated chain-arrow + sequence pill. Verification outcomes: SUCCESS=green, FAILURE=red. Template edit on dashboard.html added .disclosure-cell-{level} class hook. 205/205 tests still pass.
- **decision** 03:17 — v8.14 iteration 5: color-coded 6 dashboard panels + login masthead hidden. Agency×Algorithm matrix gets left-border by type (FEDERAL=gold, STATE=cyan, PRIVATE=green, etc.). Verification Activity bars gradient-tinted per context (gold BANKING, cyan EMPLOYMENT, violet GOV_BENEFITS, amber TRAVEL, green HEALTHCARE, steel MOTOR_VEHICLE, red VOTING). PQ Migration: green PQ pills/algorithms vs red CLASSICAL. Active Tokens: gold ID links, cyan token values, white holders, green algorithm names. Audit Events description text brightened to e8eef4. Login page masthead hidden via :has(.login-page) selector. Template edits: agency-cell, ctx-row, alg-row, token-row class hooks.
- **learning** 03:17 — CSS specificity audit before assuming source-order. Per-context bars failed first attempt because iteration-3 selector .ctx-bar-wrap > div:first-child has specificity (0,3,2) — beats plain (0,3,1) chain regardless of source order. Browser cascade: higher specificity wins, not later rule. HiddenState shadow fired (the bug is in what you didn't think to check). Fix: add .ctx-bar-wrap to selector to reach (0,4,1).
- **decision** 12:34 — v8.14 iteration 6: html-bg fix (no more white at scroll-edges), 6 distinct Token Status colors, Token Succession Lineage chain polish, cinematic ACCESS GRANTED boot overlay (1.6s fade-in/out with scan-line + progress bar + role/user sub-line + staggered dashboard reveal). Reduced-motion accessibility preserved. dashboard.html got scifi-boot-overlay div at top. polaris-scifi.css ~250 new lines for iteration 6. 205/205 integration tests still pass. The html-bg fix specifically addressed the user's pain point: over-scrolling at top/bottom of any page now shows the void gradient instead of white.
- **decision** 12:41 — v8.14 iteration 7: token detail .detail-grid <dl> now styled (was white box). overscroll-behavior: none on html + body to disallow rubber-band bounce showing white. detail-grid: cyber-blue uppercase <dt> labels, white <dd> values, gold-mono for code/mono values. 205/205 still pass.
- **decision** 12:48 — v8.14 iteration 8: LOST status pill color changed from pink to neon-amber (was wrong/jarring in lineage chain). Flash messages auto-dismiss after ~5s via CSS keyframe (max-height/padding/margin animated to 0 at end to collapse layout). Login page footer hidden via :has(.login-page) selector. Login form: autocomplete='off' on form + decoy fields to defeat Chrome/Edge autofill. Username field switched to autocomplete='off'; password to autocomplete='new-password'. Custom validation messages via oninvalid + oninput: 'OPERATOR ID REQUIRED' / 'OPERATOR ID FORMAT INVALID' / 'CREDENTIAL REQUIRED'. 205/205 tests pass.
- **decision** 13:06 — v8.14 iteration 9 SHIPPED: CSP-compliance fix discovered + completed. CSP is 'script-src self' (no unsafe-inline), so previous inline <script> blocks in login.html and base.html were silently blocked. Solution: moved login validation logic to /static/login-form.js (loaded via <script src defer>) and flash auto-dismiss to /static/flash-dismiss.js. Both work cleanly. Verified: empty submit triggers inline 'OPERATOR ID REQUIRED' error (red glow), no browser popup. Logout flash auto-removes after 4.5s. Login background softened with broad cyan radial halo + gold bottom-right accent + dimmer hex grid + 20px-blur card halo. 205/205 tests pass.
- **learning** 13:06 — Polaris CSP is strict 'script-src self' — NO unsafe-inline. Inline scripts SILENTLY FAIL (no console errors visible). Existing inline scripts in base.html (heartbeat etc.) have also been silently broken since v4 when CSP was added. Anything that needs JS must be in /static/*.js loaded with <script src=...>. Adding to DEVNOTES/known-gotchas as a recurring trap.
- **decision** 13:13 — v8.14 iteration 10: login page bg now edge-to-edge uniform. Root cause: main.content had max-width 1400px + 28px padding constraining the .login-page wrapper, plus a radial halo centered at 50%/45% creating hot-spot/dark-edge contrast. Fix: scoped main.content { max-width: none; padding: 0; min-height: 100vh } for login pages only, replaced radial halo with linear band ambient + corner gold radial, bumped card glow from inset:-40px to inset:-180px with 40px blur and 16% center alpha. The card halo now extends ~180px past its edge and dissolves seamlessly into the body bg. 205/205 tests pass.
- **decision** 13:23 — v8.14 iteration 11: collapsed 4 UC-* nav items (UC-1 Issue, UC-4 Activate, UC-5 Bind, UC-7 Audit) into single 'USE CASES' dropdown using <details>/<summary> (CSP-safe, no JS). Top nav goes from 11 items (multi-line wrap) to 8 items (single line). Dropdown panel: dark glass bg, gold top-rim, animated chevron, each item shows gold UC-x identifier + uppercase name with cyan left-border hover. Updated RoleBasedAccessControlTests::test_navigation_only_shows_allowed_links from '>UC-1 Issue<' → '>UC-1<' to match new dropdown structure. 205/205 tests pass.
- **decision** 13:29 — v8.14 iteration 12: USE CASES dropdown opaque + right-anchored + z-index elevated. Three issues fixed: (1) panel had backdrop-filter blur + 95% alpha creating see-through; replaced with solid #0a1828 → #050e1c gradient and removed backdrop-filter. (2) panel anchored left:0 overflowed viewport on narrow screens; switched to right:0 so it opens leftward from trigger. (3) Dashboard h1 text bled through because masthead and main.content both had z-index:1 — same stacking context. Fix: masthead z-index 50 + isolation:isolate, nav-uc-menu z-index 1000. Panel now correctly draws above page content with no bleed.
- **decision** 13:35 — v8.14 iteration 13: Atlas globe sci-fi polish. CSS-only additions targeting existing SVG element classes (.d3-globe-sphere/limb/grid/land/borders/labels/nodes). Added: animated 2-layer starfield (radial gradient stars + drifting layer, 6s twinkle + 90s drift), 24px+64px stacked drop-shadow atmosphere glow on sphere, 8px+18px cyan glow on the limb rim, brighter graticule (cyan 12%), country borders with subtle glow, country fills rgba(18,36,64,0.88), monospace tinted country labels with text-shadow, enhanced reticle drop-shadow on hover (4+8+22px stacked glow), node-arrival keyframes (1.4s), gold-tinted selection brackets with glow, four-corner HUD accent borders (TL/BR cyber + TR/BL gold), and animated classification-strip pulse (4s ease). Pure CSS, no JS changes, no test impact. 205/205 tests still pass.
- **learning** 13:43 — v8.14 atlas globe iteration 14: stage overflow:hidden was clipping SVG drop-shadow atmospheric glow. Moved cyan halo into stage radial-gradient bg (lives inside bounds, can't be clipped). Sphere fill darkened (6,18,38), land lightened to (28,54,92) for stronger continent silhouettes. Limb brightened to rgba(140,230,255,0.78). Labels bumped 0.55→0.82 opacity with triple text-shadow + 0.06em letter-spacing. Visible label count went 10→30 via preview inspection. 199/199 tests pass.
- **decision** 14:01 — drafted proposals/R11-6-issuer-discretion-bounds.md per Architect's top suggestion (arch-2026-05-11-001); MEDIUM propose-and-wait; defaults N=5%/W=30d/co-signer=any BOTH agency != actor
- **decision** 14:12 — R11-6 proposal refined per Architect alignment review: (1) wired uc8_revoke_token into RevocationList per UC-4 pattern, (2) added pg_advisory_xact_lock(agency_id) + C9 real-threading concurrency tests, (3) added 'constitutional limits on issuer discretion' framing section explicit against 'Polaris is NOT an authority'
- **decision** 14:36 — SHIPPED R11-6 / M2-11 (issuer-discretion bounds) as v8.15. uc8_revoke_token procedure + IssuerDiscretionPolicy table + advisory-lock concurrency + belt-and-suspenders trigger + 11 IssuerDiscretionBoundsTests + 2 ConcurrencyTests (race the boundary, cross-agency parallelism). Python 247 (quick 212 PASS), SQL 79 (43+J PASS), ai-done 9 pass / 2 warn / 0 fail. PDF §9 constitutional limits leg now closed; v2 done-list 5/12 (M2-3, M2-4, M2-5, M2-11, M2-12).
- **decision** 14:45 — Architect recommended R11-4 (tiered enrollment / M2-9) as the next move. Pre-cleanup: renamed R11-2's UC-8 references to UC-9 (UC-8 slot taken by R11-6). Drafted proposals/R11-4-tiered-enrollment.md with full alignment audit including the explicit second-best-attack analysis (NOT_ENROLLED weaponized as surveillance marker) and the asymmetric design (EXEMPT frictionless, mass-enumeration deliberate). MEDIUM propose-and-wait.
- **decision** 15:01 — SHIPPED R11-4 / M2-9 (tiered enrollment / population coverage) as v8.16. EnrollmentStatusEvent table + IndividualCurrentEnrollment view + seed_default_enrollment_status trigger + civic_enrollment_summary function + 10 TieredEnrollmentTests + 5 SQL self-tests section L. Asymmetric design (EXEMPT frictionless, mass-NOT_ENROLLED enumeration deliberate) defends against the surveillance-marker second-best attack. Python quick 222 PASS, SQL 48 PASS, ai-done 9 pass / 2 warn / 0 fail. PDF §9 Population coverage leg closed; v2 done-list 6/12. The schema-doesnt-weaponize-itself-against-the-holder triad: entry (R11-4) and exit (R11-6) shipped; recovery (R11-2, now UC-9) remains.
- **decision** 15:14 — Documentation sweep after R11-4 ship: CLAUDE.md / README.md / polaris_sql/README.md / polaris_web/README.md / polaris_cli/README.md / docs/INSTALL.md / docs/README.md / docs/DATA-MODEL.md / 04_data.sql comment block all updated with v8.16 counts (16 tables, 257 Python tests across 44 classes, 87 SQL self-tests across 3 files, 5 UC stored procedures, UC-8 references throughout). No regressions: 48/48 SQL + 222/222 Python quick all green.
- **decision** 15:27 — R11-2 alignment audit + refinement pass: folded in 6 refinements before implementation: (1) C9 pg_advisory_xact_lock on claimed_individual_id with cross-individual parallelism preserved + 2 ConcurrencyTests entries; (2) RevocationList INSERTs for each LOST token in APPROVED branch (UC-4 pattern, prevents CRL/state divergence); (3) administrative cool-down vs operational grace-period framing per PDF §9.1 — administrative window in v1, operational TemporaryAttestation deferred with rationale; (4) explicit 'schema constrains shape, agencies decide' section completing the holder-protection triad framing; (5) audit-row tag format [RECOVERY:<id>] specified; (6) admin role required for deciding-user (schema CHECK + Flask @require_role) instead of new security-officer role. Acceptance criteria up to 16 items; ≥13 + ≥2 concurrency tests required. Ready for VANTA yes-do.
- **decision** 15:48 — SHIPPED R11-2 / M2-7 (catastrophic-loss recovery / UC-9) as v8.17. RecoveryRequest table + 4 CHECK constraints + partial unique index + uc9_initiate_recovery + uc9_complete_recovery (admin-only, RAISE EXCEPTION enforced) + pg_advisory_xact_lock on claimed_individual_id + 3 Flask routes + 3 templates + 15 CatastrophicLossRecoveryTests + 2 ConcurrencyTests + 5 SQL self-tests in section M. Python quick 239 PASS, SQL 97 PASS. THE TRIAD IS COMPLETE: R11-4 (entry) + R11-6 (exit) + R11-2 (recovery). v2 done-list 7/12. Schema-doesn't-weaponize-itself-against-the-holder is now structurally enforced across all three identity-as-coercion failure modes the PDF §9 names.
- **decision** 15:55 — R11-1 alignment audit + refinement pass: folded 7 refinements before implementation. (1) pg_advisory_xact_lock(hashtext('polaris.migrate.'||token_id)) on uc6_migrate_algorithm — per-token serialization; (2) targeted append-only trigger enforce_token_signature_immutability — DELETE forbidden, UPDATE confined to one-way deprecation_date; (3) explicit verify+migrate consistency-model test (transaction snapshot semantics); (4) triad-completion framing — R11-1 closes cryptographic-diversity leg of PDF §9 issuer-trust-concentration alongside R11-6 and M2-8; (5) 'schema records, agencies decide' section paralleling R11-6/R11-4/R11-2; (6) anti-auto-derivation explicit — TokenSignature.deprecation_date NOT auto-set from CryptographicAlgorithm.deprecation_date; (7) TokenSignature IS the migration audit-of-record (no separate event table needed). Acceptance criteria up to 19 items; ≥16+3 tests. Ready for VANTA yes-do.
- **decision** 17:35 — SHIPPED R11-1 / M2-6 (multi-signature transitional state / UC-6) as v8.18. TokenSignature M:N table + 2 invariant triggers (active-signature + immutability with one-way deprecation_date) + uc6_migrate_algorithm procedure with pg_advisory_xact_lock(token_id) + UC-1/UC-9 extensions to insert TokenSignature alongside new IdentityToken + backfill block + 16 MultiSignatureTests + 3 ConcurrencyTests (per-token race, verify+migrate snapshot consistency, cross-token parallelism) + 5 SQL self-tests section N. Python quick 258 PASS, SQL 58 PASS. PDF §9.4 cryptographic-migration leg closed. Issuer-trust-concentration triad 2/3 done (R11-1 + R11-6); M2-8 federation is the only unbuilt leg across both PDF §9 triads. v2 done-list 8/12.
- **decision** 17:50 — SHIPPED v8.19 — the Sanctum, formal agent-operator strategic-consultation protocol. Named by VANTA (picked between 'Sanctum' and 'Nexus'). Components: meta/sanctum-protocol.md (full spec with entry triggers, anti-patterns, lineage), scripts/ai-sanctum.sh (open/close/list/--voice; refuses incomplete preparation in --strict mode), sanctum/ directory with README + 7-section document template, meta/sanctum-index.md (chronological index with pattern observations across past sessions). Backfilled the v8.15-v8.18 ships (R11-6, R11-4, R11-2, R11-1) as canonical Sanctum records so future agents have a corpus to learn from. Triggers reserved for MEDIUM/HIGH propose-and-wait + cross-arc + structural cognitive-layer changes; routine LOW-risk work proceeds without a Sanctum (anti-larp guardrail). Wired into CLAUDE.md (timeline + file map + when-to-enter callout) and ai-help.sh Synthesis & reporting group. Open-then-close cycle verified end-to-end including the meta/sanctum-index.md insertion fix.
- **decision** 18:09 — SHIPPED v8.20 — Sanctum self-monitoring + audit-of-record principle + Architect reflection. Three artifacts: DEVNOTES/audit-of-record.md (canonical principle, four current instances, conformance grading), ai-meta.sh check_sanctum (CM check #6 — first extension since CM was defined in v8.9), ai-architect.sh --reflect extended to ingest closed/rejected Sanctums with prediction-vs-reality analysis (default last 10, --reflect-n N configurable). Plus 4 new structural-invariant tests (TestSanctumIntegrity). The reflection mechanism caught its own missing-link gap on first run — the learning loop functioned correctly on cycle 1. First Sanctum created UNDER the protocol rather than backfilled. ai-meta clean, link-check 29/29, structural tests 47/47, all green. The Sanctum cannot drift into ceremony because three different layers now monitor it.
- **decision** 18:29 — R10-2 alignment audit + refinement pass: folded 6 refinements before implementation. (1) pg_advisory_xact_lock(hashtext('polaris.anchor.close-batch.'||algorithm_id)) on close_anchor_batch — per-algorithm serialization, cross-algorithm parallel; (2) two ConcurrencyTests entries (per-algorithm race, cross-algorithm parallelism); (3) AnchorBatch positioned as the 5th audit-of-record instance, extending DEVNOTES/audit-of-record.md; (4) Substrate-D arc closure framing (after R10-2, arc is 4/5 done; M2-1 ZK-SNARK is the last unbuilt leg); (5) anti-auto-derivation note — committed_to_chain is operator-set, NOT auto-derived; (6) 'schema implements a primitive, operator decides timing' section — distinct posture from R11-x ships (those constrain agency behavior; R10-2 implements a cryptographic primitive). Acceptance up to 13 items; ≥13+2 tests. Ready for VANTA yes-do via Sanctum.
- **decision** 23:49 — v8.26 folder reorg shipped: 9 per-ship DEVNOTES moved to ships/ subfolder, 8 cross-cutting stayed at root. DEVNOTES/README.md + journal/INDEX.md + SEED_DATA.md added as navigation indexes. 24 active-reference files rewritten; sanctum/proposals/journal (pre-INDEX)/old CHANGELOG retain pre-v8.26 paths as audit-of-record. ai-link-check 58/58 clean.
- **learning** 23:59 — Pattern-recap for v8.21–v8.26 (retroactive tag pass). ai-meta flagged 11/22 cold patterns; investigation shows the catalog isn't premature — the matches genuinely fired but were never tagged. Honest matches: **Foundation** (every M2-* added schema: AnchorBatch, AgencyTrustAttestation, TokenStateEpoch, TokenStateEpochLeaf, DuressEvent — five new tables in the v2 arc). **Convention** (patterns/ + ai-pattern.sh enforce the 22-shape closure; following the established Sanctum protocol on every MEDIUM-risk ship). **ShipPressure** (2026-05-11 alone shipped 12 mission items + 6 Sanctums; v8.21→v8.26 in a single day). **Endurance** (35+ journal entries on a single day; mid-day Architect persona consultations across M2-1 exploration → ship → M2-10 → folder optimization). **Recurrence** (Postgres-restart-between-bash-turns recurs in CLAUDE.md gotchas; admin-test-lockout recurs; the AppUser forward-reference FK has lurked since v8.17 and recurred at v8.26 Docker first-run). **Inversion** (M2-1 used the exploration-Sanctum-first variant — inverting the normal ship-then-document cadence because the cryptographic design space was 36 combinations wide). **Workaround** (Plonky2 `pad_leaves_to_full_depth()` helper as workaround for the tree-depth API quirk where `tree.prove(i)` returns siblings of actual tree depth, not circuit depth — clean workaround at the call site, not in the library). **Collapse** (issuer-trust-concentration triad R11-1 + R11-6 + R11-3 collapsed to a single closed concern at v8.22; holder-protection triad R11-4 + R11-6 + R11-2 collapsed at v8.17). **Phantom** (the BiometricEnrollment-table phantom-reference catch from v8.12 is the canonical Polaris example; the v8.20 doc↔schema executable test is the institutional response). **Clarity** (audit-of-record principle expanded 4 → 8 instances across the v2 arc; the principle became *visible* as a generalizable pattern at v8.20 when DEVNOTES/audit-of-record.md was canonicalized). **Reckoning** (the v8.17 AppUser-FK schema decision came due at v8.26 when the user ran `Polaris.command` fresh — the cost of forward-reference DDL paid as a Docker first-run failure; promoting AppUser into 01_schema.sql was the reckoning). All 11 patterns warm post-this-entry.
- **decision** 23:59 — v8.27 cognitive-layer self-tightening shipped: v1 trio ✗ RETIRED with audit annotations; 3 scripts updated (ai-status/ai-snapshot/ai-propose) for the symbol change with back-compat regex; ai-architect.sh game_type_for() expanded 12→24 R-ids (full ROADMAP coverage); ai-done.sh +check #12 wires --save unconditionally so --reflect has substrate; pattern catalog 11→22 warm via honest retroactive recap. Architect's 4 autonomous items closed. v3-vs-steady-state binding question surfaced for VANTA, not decided here.
