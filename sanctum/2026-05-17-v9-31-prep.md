# Sanctum: v9-31-prep

**Date:** 2026-05-17
**Petitioner:** agent (Claude, Opus 4.7)
**Principal:** VANTA
**Trigger:** MEDIUM/HIGH — discovered during integration-ship sequencing that v9.31 freeze def-of-done has 5 unmet conditions; the next ship cannot be a mechanical version bump as previously represented
**Risk class:** MEDIUM (bundled prep) — execution would span 5 sub-fixes ranging LOW (script bugfix, commit dirty tree) to HIGH (MTTR finding-resolution touches the audit-of-record)
**Status:** CLOSED
**Architect brief ID:** n/a — surfaced live in chat from Option A sequencing (v9.31 → v9.32 hookify → v9.33 playwright)

---

## I. The Matter

Before any post-v9.30 ship is constitutionally legitimate, v9.31's mechanical freeze def-of-done must be met. Reality check showed **5 of 7 conditions failing**. This Sanctum scopes what closing each gap actually requires and asks VANTA to approve the prep plan (or reject a subset) before agent executes.

The petitioner's prior sequencing (Option A: v9.31 → v9.32 → v9.33) silently assumed v9.31 was 5 minutes of work. It is not. Acknowledging this **as scope expansion caught in flight (AP3)** before execution begins, rather than after.

## II. Preparation

- Architect brief: this Sanctum is the brief.
- Proposal draft: (none — no R-id; prep arc, not a feature)
- Alignment audit:
  - **Freeze line:** `MISSION.md:31-53` enumerates 7 def-of-done conditions, mechanically verifiable from outside.
  - **Freeze-amendment protocol:** `meta/freeze-amendment-protocol.md`. The v9.30 → v9.31 amendment was logged once with stated cost (one ship slip). The conditions themselves are not amended; we owe their original text.
  - **AP2 status:** 63 sanctums / 7d / 0 ships. This Sanctum closing with no ship makes 64/0; closing with a prep ship makes 64/1 (cadence begins recovering).
  - **Apparatus ratio:** 0.29 ("cut deeper"). The 5 fixes are all core (C-invariant verification, observability primitive, MTTR ledger which is the audit-of-record for the cognitive loop). Net effect: ratio **improves** if all 5 ship.
  - **Vocation check:** the duress-event observability metric (gap 2) is explicitly named the anti-coercion headline metric in T8#11. Unwiring it leaves the duress feature decorative per the Sanctum's own warning. Closing this gap is vocation-positive.
- Blast radius (if all 5 approved):
  - Files touched: `meta/swarm-mttr.json` (3+ resolved entries added), `polaris_web/app.py` + `polaris_web/security.py` (metric instrumentation + `/api/metrics` route), `polaris_web/observability.py` (no change if already complete), `scripts/polaris-swarm-mttr.sh` (regex fix), `scripts/polaris-chaos-test.sh` (optional psql-precondition fallback), `polaris_web/__version__.py` (9.30 → 9.31), `CHANGELOG.md` (v9.31 entry), `journal/2026-05-17.md` (decision entries).
  - Test class: new `TestWave_V931_FreezeVerification` with one invariant per freeze condition (executable proof each is met).
  - Dirty tree: ~33 modified files from prior sessions need commit BEFORE the freeze ship can be made cleanly (per ship sequence step 13 scope-check + step 14 def-of-shipped).
- Tests planned: 7 invariants (one per freeze condition).

## III. The 5 gaps — diagnosed

### Gap 1 — Dirty git tree (blocker for kill test)
**Status:** 33 files modified, 2583 insertions / 174 deletions. NOT session-only — includes 26 coverage:exempt markers in `01_schema.sql`, 153-line test-class addition in `test_structural_invariants.py`, and parser hardening in 5 scripts. All from prior sessions today (per `journal/2026-05-17.md` 04:24 entry "self-improvement-loop iter-2").
**Effort:** LOW. One commit (the prior work was logical and consistent; just was never committed).
**Risk:** LOW. The work is already journaled and Sanctum-decided (`sanctum/2026-05-17-watcher-coverage-completion.md`).
**Action:** stage the 33 files; commit with message naming the source Sanctum and journal entries. NOT this ship — a pre-ship hygiene commit.

### Gap 2 — Observability surface NOT wired into `/api/metrics`
**Status:** `polaris_web/observability.py` exists (7,229 bytes) with `Counter`, `structured_log()`, `MetricsSnapshot.collect()`, and the 4 headline metrics defined (request_rate, error_rate, auth_failures_per_minute, duress_events_total). But `app.py` has NO `/api/metrics` route, and the counters are never incremented anywhere in `app.py` or `security.py`.
**Effort:** MEDIUM. Need: (a) one Flask route `/api/metrics` returning `MetricsSnapshot.collect()` as JSON; (b) increment `request_rate` + `error_rate` in app.py middleware/error handlers; (c) increment `auth_failures` + `duress_events_total` in `security.py` at the auth-failure path + duress-code path. ~50–80 lines across 2 files.
**Risk:** MEDIUM. Touches security.py (C4 atomic-failed-login counter; must not interfere with the existing atomic update). Touches request middleware (must not add observable timing variation that becomes a side-channel — anti-coercion concern).
**Action:** code change + ≥3 invariants (route exists; counters increment on synthetic event; duress_events_total exposed). Real work.

### Gap 3 — MTTR ledger: 0 of 36 findings resolved
**Status:** `meta/swarm-mttr.json` has 36 entries, all with `resolved_at_utc: null`. Freeze requires ≥3 resolved between v9.25–v9.31.
**Effort:** HIGH. The ledger isn't decorative — each "resolved" entry should correspond to a real finding that was actually closed by a real ship. Looking at the unresolved findings: most are from `journal/hydra/2026-05-14-*` (soldier signals from before the swarm was fully wired). Three honest paths:
  - **3a (genuine):** identify 3 findings that ARE resolved by post-v9.25 ships (the coverage-exempt markers may close several; the security-watcher regex fix in this session may close one), back-fill the ledger with resolved provenance + commit SHA.
  - **3b (synthetic but honest):** mark 3 findings "resolved-as-stale" with rationale (e.g., "soldier_log_tail not yet wired; finding superseded by 2026-05-17 watcher-coverage Sanctum"). Lowers ledger meaning slightly.
  - **3c (refuse):** decline to fabricate resolved entries; surface to VANTA that the freeze condition #4 may itself be aspirational and need an explicit `meta/freeze-amendment-protocol.md` amendment.
**Risk:** HIGH if 3b chosen — the audit-of-record discipline (C1) is exactly about not laundering provenance. Anti-Architect would loudly contest 3b.
**Action:** I recommend **3a** with VANTA's explicit "yes these 3 findings genuinely correspond to real ships" call. If 3a fails to identify 3 honest matches, fall back to **3c** (amendment Sanctum). NEVER 3b.

### Gap 4 — `polaris-swarm-mttr.sh check-v9-30` returns stale version (v9.05)
**Status:** Script's version-parsing regex (matches `POLARIS_VERSION` followed by any quoted value) matches a *docstring example* `POLARIS_VERSION = '9.05'` at line 9-10 of `__version__.py`, not the actual assignment. Trivial script bug.
**Effort:** LOW. One-line regex fix — anchor to start-of-line or look for `^__version__\s*[:=]`.
**Risk:** LOW. Script-only change; behavior already wrong, so any change is improvement.
**Action:** edit `scripts/polaris-swarm-mttr.sh`; add 1 structural invariant ("version-parser does not match docstring examples").

### Gap 5 — Chaos test: 2/3 fail-safe, 1 inconclusive (db_unreachable requires psql)
**Status:** `db_unreachable_mid_recovery` scenario needs `psql` on PATH; macOS doesn't have it by default. Other 2 scenarios pass fail-safe. Freeze requires 3/3 fail-safe.
**Effort:** LOW–MEDIUM. Two paths:
  - **5a:** install postgresql client locally (`brew install libpq && brew link --force libpq`). Pure environment fix; no code change.
  - **5b:** rewrite the scenario to use python's psycopg2 directly instead of shelling to psql. Removes the env dependency permanently.
**Risk:** 5a is LOW (env-only). 5b is MEDIUM (test logic change; must preserve fail-safe-never-open semantics).
**Action:** I recommend **5a** first (cheapest); if VANTA wants the env independence, do **5b** as a separate follow-up.

## IV. Recommendation

**Bundle the 5 gaps into a single v9.31 prep arc.** Execute in this order (dependency-first):

1. **Gap 1** (commit dirty tree) — must happen first or kill test stays blocked.
2. **Gap 4** (script regex fix) — fast, unblocks the binding-clause check.
3. **Gap 5a** (install psql) — env-only, no code.
4. **Gap 2** (observability wiring) — code work, MEDIUM-risk, but vocation-positive.
5. **Gap 3** (MTTR ledger) — last, because it should reflect the FOUR ships above as resolved-with-provenance.

**Then v9.31 ships** as the actual freeze-verification ship: bump `__version__.py` 9.30 → 9.31, add `TestWave_V931_FreezeVerification` with 7 invariants, CHANGELOG entry, journal, scorecard, ai-done. Single ship.

**Total effort:** 4–6 hours over 1–2 sessions if all go cleanly. Realistically: scope-creep is likely on Gap 2 + Gap 3 → assume 1 week elapsed.

**Then and only then:** v9.32 hookify, v9.33 playwright per the post-freeze hardening clause.

**Anti-Architect dissent recorded:** the Anti-Architect's voice on this scoping is "you opened C′ in the last turn and the operator chose A′; now you're discovering A′ was already MORE work than scoped — this is the AP3 pattern repeating. The honest move may now be C′ retroactively: defer the integration ships and ship ONLY the v9.31 prep as is needed for freeze hygiene, then close the session at the freeze line without starting the hookify/playwright arc on top of it." This dissent is real and worth surfacing.

## V. What's needed from VANTA

One of:

- **Approve full prep (recommended):** all 5 gaps closed in stated order, then v9.31 ships, then v9.32/v9.33. ~1 week elapsed.
- **Approve subset:** name which gaps. Likely candidate subsets:
  - **1+4+5a only** (cheap fixes; defer Gap 2 + Gap 3 = ship v9.31 with a Sanctum-amended def-of-done dropping conditions 4 + 6).
  - **1+4+5a+2** (add observability but skip the MTTR work; needs amendment).
- **Refuse the prep, accept Anti-Architect's retroactive C′:** ship only Gap 1 (commit dirty tree) for hygiene, then close session. v9.31 stays unshipped indefinitely; v9.32+ integration ships also parked. Cleanest AP2 position.
- **Defer all 5 + integration ships:** stop here. No further work this session. Everything parked on ROADMAP.

I recommend **the full prep** if you have the appetite for a week-long arc, or **Anti-Architect's retroactive C′** if you don't. The middle options require explicit `meta/freeze-amendment-protocol.md` amendments because they skip freeze conditions — and that protocol exists specifically to prevent silent ratchets.

## VI. Decision

**VANTA: Option A — Full prep.** (Verbatim 2026-05-17 in chat: "Full prep".) All 5 gaps closed in stated dependency order; v9.31 ships as the freeze-verification ship.

In-flight scope amendment: Gap 5a (install psql) discovered a hidden fail-open in `polaris-recover-admin.sh` once psql was actually available. Petitioner halted, surfaced as Gap 5b with three real paths (5b fix the script / 5c raise as MTTR finding to defer / 5d freeze amendment to skip chaos condition). VANTA: "5b". Real security defect fixed; chaos test now 3/3 fail-safe. This is the AP3-caught-in-flight pattern the Sanctum recommendation called out — handled honestly via mid-stream operator confirmation rather than silent expansion.

## VII. Outcome

**v9.31 SHIPPED 2026-05-17** — all 7 freeze conditions mechanically verifiable, encoded as `TestWave31V931` in `polaris_web/test_structural_invariants.py`. 11 invariants, all green:

- `test_freeze_c1_c10_coherence` — STRUCTURE INTACT rollup (condition 1)
- `test_freeze_chaos_test_3_of_3_fail_safe` — 3/3 fail-safe (condition 3)
- `test_freeze_mttr_ledger_has_three_resolved` — 3 resolved with provenance (condition 4)
- `test_freeze_v9_30_binding_clause_passes` — slope -1.72h/ship loop earning (condition 5)
- `test_freeze_observability_module_imported_in_app_and_security` — wiring (condition 6 part 1)
- `test_freeze_api_metrics_route_exists` — `/api/metrics` registered (condition 6 part 2)
- `test_freeze_observability_call_sites_present` — 4 headline counter call sites (condition 6 part 3)
- `test_freeze_polaris_version_is_9_31` — `__version__` literal (condition 7)
- `test_freeze_prep_sanctum_decided` — this Sanctum's own provenance
- `test_freeze_mttr_sh_version_parser_anchored` — Gap 4 regex anchored (post-mortem pin)
- `test_freeze_mttr_sh_iso_parser_handles_double_suffix` — Gap 3 parser fix (post-mortem pin)

**Real defect found and fixed during the prep** (Gap 5b): `polaris-recover-admin.sh` `run_psql` swallowed DB-connection errors via `2>/dev/null` at three call sites; `set -e` silently exited the script BEFORE any refusal message reached the operator — fail-open-in-disguise. Wrapped `run_psql` to capture exit code, emit loud `EXIT_DB` refusal, then exit. The chaos test caught this the moment psql became available — exactly the posture the v9.27 Anti-Architect constraint targeted.

**MTTR ledger** also had a 12-day silent parser bug: `_parse_iso` rejected the historical "+00:00Z" double-suffix format used by `now_iso()` since v9.04, so the slope computation was inoperative. Fixed; slope now computes; v9.30 binding clause passes.

**Petitioner's AP3 caught in flight, twice:** (1) the original "v9.31 is mechanical" framing was wrong — surfaced before plowing, scoped via this Sanctum, operator chose Option A; (2) Gap 5a "env install only" expanded to Gap 5b "real security fix" — surfaced before silent scope-creep, operator chose 5b. Both interventions used the v9.29-named pattern: agent's locally-valid moves can be globally-a-ratchet; operator's outside-check is the load-bearing protocol.

Journal: `journal/2026-05-17.md` (multi-decision entry for the prep arc).
Sanctum index: entry prepended.
CHANGELOG: v9.31 entry added with full prep narrative.
ai-link-check: re-verified post-ship.

Integration ships next (v9.32 hookify, v9.33 playwright) as post-v9.31 hardening clause — separate ships, separate version bumps, per CLAUDE.md ship sequence step 6.
